
load @textsurf/loom
  find mill code
  find mill book

file ../book/**/*.link
  bind role, loan book

file ../code/javascript/**/*.link
  bind role, loan code
  bind dock, loan js

file ../code/browser/**/*.link
  bind role, loan code
  bind dock, loan js

file ../code/node/**/*.link
  bind role, loan code
  bind dock, loan js
