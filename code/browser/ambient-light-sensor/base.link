
task build-ambient-light-sensor
  base options

  call make
    bind constructor, text <AmbientLightSensor>
    bind factor, link options
    turn seed

task get-illuminance
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <illuminance>
    turn seed

task set-illuminance
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <illuminance>
    bind factor, link value
