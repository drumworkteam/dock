
task get-timing
  base build

  cause call-base
    bind object, link build
    bind method, text <getTiming>

task get-computed-timing
  base build

  cause call-base
    bind object, link build
    bind method, text <getComputedTiming>
    turn seed

task update-timing
  base build
  base timing

  cause call-base
    bind object, link build
    bind method, text <updateTiming>
    bind factor, link timing
    turn seed
