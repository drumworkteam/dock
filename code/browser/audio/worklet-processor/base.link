
task build-audio-worklet-processor
  base options

  call make
    bind constructor, text <AudioWorkletProcessor>
    bind factor, link options
    turn seed

task get-port
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <port>
    turn seed
