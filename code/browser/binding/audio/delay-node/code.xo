
task build-delay-node
  base context
  base options

  call make
    bind constructor, text <DelayNode>
    bind factor, link context
    bind factor, link options
    turn seed

task get-delay-time
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <delayTime>
    turn seed
