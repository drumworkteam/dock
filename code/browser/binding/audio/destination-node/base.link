
task get-max-channel-count
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <maxChannelCount>
    turn seed

task set-max-channel-count
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <maxChannelCount>
    bind factor, link value
