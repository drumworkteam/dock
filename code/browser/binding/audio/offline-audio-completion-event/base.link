
task build-offline-audio-completion-event
  base type
  base init

  call make
    bind constructor, text <OfflineAudioCompletionEvent>
    bind factor, link type
    bind factor, link init
    turn seed

task get-rendered-buffer
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <renderedBuffer>
    turn seed
