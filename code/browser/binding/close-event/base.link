
task build-close-event
  call make
    bind constructor, text <CloseEvent>
    turn seed

task get-code
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <code>
    turn seed

task get-reason
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <reason>
    turn seed

task get-was-clean
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <wasClean>
    turn seed
