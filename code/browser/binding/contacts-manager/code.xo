
task select
  base build
  base properties
  base options

  cause call-method
    bind object, link build
    bind method, text <select>
    bind factor, link properties
    bind factor, link options
    turn seed

task get-properties
  base build

  cause call-method
    bind object, link build
    bind method, text <getProperties>
    turn seed
