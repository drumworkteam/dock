
task get-name
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <name>
    turn seed

task set-name
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <name>
    bind factor, link value

task get-hash
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <hash>
    turn seed

task set-hash
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <hash>
    bind factor, link value

task get-salt
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <salt>
    turn seed

task set-salt
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <salt>
    bind factor, link value

task get-info
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <info>
    turn seed

task set-info
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <info>
    bind factor, link value
