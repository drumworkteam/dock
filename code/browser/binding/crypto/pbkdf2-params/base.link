
task get-name
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <name>
    turn seed

task set-name
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <name>
    bind factor, link value

task get-hash
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <hash>
    turn seed

task set-hash
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <hash>
    bind factor, link value

task get-salt
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <salt>
    turn seed

task set-salt
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <salt>
    bind factor, link value

task get-iterations
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <iterations>
    turn seed

task set-iterations
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <iterations>
    bind factor, link value
