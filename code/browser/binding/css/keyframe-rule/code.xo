
task get-key-text
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <keyText>
    turn seed

task set-key-text
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <keyText>
    bind factor, link value

task get-style
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <style>
    turn seed
