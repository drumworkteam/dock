
task get-max
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <max>
    turn seed

task get-min
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <min>
    turn seed

task get-value
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <value>
    turn seed
