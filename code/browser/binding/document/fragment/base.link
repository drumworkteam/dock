
task build-document-fragment
  call make
    bind constructor, text <DocumentFragment>
    turn seed

task get-children
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <children>
    turn seed

task get-first-element-child
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <firstElementChild>
    turn seed

task get-last-element-child
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <lastElementChild>
    turn seed

task get-child-element-count
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <childElementCount>
    turn seed

task query-selector
  base build
  base selectors

  cause call-base
    bind object, link build
    bind method, text <querySelector>
    bind factor, link selectors

task query-selector-all
  base build
  base selectors

  cause call-base
    bind object, link build
    bind method, text <querySelectorAll>
    bind factor, link selectors

task get-element-by-id
  base build
  base selectors

  cause call-base
    bind object, link build
    bind method, text <getElementById>
    bind factor, link selectors
