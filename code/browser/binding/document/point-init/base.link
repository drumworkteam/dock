
task get-x
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <x>
    turn seed

task set-x
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <x>
    bind factor, link value

task get-y
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <y>
    turn seed

task set-y
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <y>
    bind factor, link value

task get-z
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <z>
    turn seed

task set-z
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <z>
    bind factor, link value

task get-w
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <w>
    turn seed

task set-w
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <w>
    bind factor, link value
