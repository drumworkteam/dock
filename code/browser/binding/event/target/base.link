
task build-event-target
  call make
    bind constructor, text <EventTarget>
    turn seed

task add-event-listener
  base build
  base type
  base listener
  base options
  base use-capture
  base use-capture
  base wants-untrusted

  cause call-base
    bind object, link build
    bind method, text <addEventListener>
    bind factor, link type
    bind factor, link listener
    bind factor, link options
    bind factor, link use-capture
    bind factor, link use-capture
    bind factor, link wants-untrusted
    turn seed

task remove-event-listener
  base build
  base type
  base listener
  base options
  base use-capture
  base use-capture

  cause call-base
    bind object, link build
    bind method, text <removeEventListener>
    bind factor, link type
    bind factor, link listener
    bind factor, link options
    bind factor, link use-capture
    bind factor, link use-capture
    turn seed

task dispatch-event
  base build

  cause call-base
    bind object, link build
    bind method, text <dispatchEvent>
    turn seed
