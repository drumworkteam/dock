
task build-federated-credential
  base init

  call make
    bind constructor, text <FederatedCredential>
    bind factor, link init
    turn seed

task get-provider
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <provider>
    turn seed

task get-protocol
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <protocol>
    turn seed
