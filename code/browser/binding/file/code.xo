
task build-file
  base bits
  base name
  base options

  call make
    bind constructor, text <File>
    bind factor, link bits
    bind factor, link name
    bind factor, link options
    turn seed
