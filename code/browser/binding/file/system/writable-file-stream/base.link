
task write
  base build
  base data

  cause call-base
    bind object, link build
    bind method, text <write>
    bind factor, link data
    turn seed

task seek
  base build
  base position

  cause call-base
    bind object, link build
    bind method, text <seek>
    bind factor, link position
    turn seed

task truncate
  base build
  base size

  cause call-base
    bind object, link build
    bind method, text <truncate>
    bind factor, link size
    turn seed
