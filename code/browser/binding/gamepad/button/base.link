
task get-value
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <value>
    turn seed

task get-pressed
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <pressed>
    turn seed
