
task get-clear
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <clear>
    turn seed

task set-clear
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <clear>
    bind factor, link value
