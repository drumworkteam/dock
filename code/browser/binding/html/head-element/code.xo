
task get-profile
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <profile>
    turn seed

task set-profile
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <profile>
    bind factor, link value
