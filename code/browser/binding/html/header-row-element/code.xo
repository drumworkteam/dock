
task get-align
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <align>
    turn seed

task set-align
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <align>
    bind factor, link value

task get-color
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <color>
    turn seed

task set-color
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <color>
    bind factor, link value

task get-noshade
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <noshade>
    turn seed

task set-noshade
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <noshade>
    bind factor, link value

task get-size
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <size>
    turn seed

task set-size
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <size>
    bind factor, link value

task get-width
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <width>
    turn seed

task set-width
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <width>
    bind factor, link value
