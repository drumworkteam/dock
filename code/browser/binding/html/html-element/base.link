
task get-version
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <version>
    turn seed

task set-version
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <version>
    bind factor, link value
