
task get-reversed
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <reversed>
    turn seed

task set-reversed
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <reversed>
    bind factor, link value

task get-start
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <start>
    turn seed

task set-start
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <start>
    bind factor, link value

task get-type
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <type>
    turn seed

task set-type
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <type>
    bind factor, link value

task get-compact
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <compact>
    turn seed

task set-compact
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <compact>
    bind factor, link value
