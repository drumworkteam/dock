
task get-abbr
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <abbr>
    turn seed

task set-abbr
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <abbr>
    bind factor, link value

task get-cell-index
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <cellIndex>
    turn seed

task get-col-span
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <colSpan>
    turn seed

task set-col-span
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <colSpan>
    bind factor, link value

task get-headers
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <headers>
    turn seed

task get-row-span
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <rowSpan>
    turn seed

task set-row-span
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <rowSpan>
    bind factor, link value

task get-scope
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <scope>
    turn seed

task set-scope
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <scope>
    bind factor, link value
