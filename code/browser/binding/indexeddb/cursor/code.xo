
task get-source
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <source>
    turn seed

task get-direction
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <direction>
    turn seed

task get-key
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <key>
    turn seed

task get-primary-key
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <primaryKey>
    turn seed

task get-request
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <request>
    turn seed

task advance
  base build
  base count

  cause call-method
    bind object, link build
    bind method, text <advance>
    bind factor, link count

task continue
  base build
  base key

  cause call-method
    bind object, link build
    bind method, text <continue>
    bind factor, link key

task continue-primary-key
  base build
  base key
  base primary-key

  cause call-method
    bind object, link build
    bind method, text <continuePrimaryKey>
    bind factor, link key
    bind factor, link primary-key

task delete
  base build

  cause call-method
    bind object, link build
    bind method, text <delete>

task update
  base build
  base value

  cause call-method
    bind object, link build
    bind method, text <update>
    bind factor, link value
    turn seed
