
task build-install-event
  base type
  base init

  call make
    bind constructor, text <InstallEvent>
    bind factor, link type
    bind factor, link init
    turn seed

task get-active-worker
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <activeWorker>
    turn seed
