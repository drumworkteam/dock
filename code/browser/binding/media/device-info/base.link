
task get-device-id
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <deviceId>
    turn seed

task get-group-id
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <groupId>
    turn seed

task get-kind
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <kind>
    turn seed

task get-label
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <label>
    turn seed
