
task build-media-stream-audio-destination-node
  base context
  base options

  call make
    bind constructor, text <MediaStreamAudioDestinationNode>
    bind factor, link context
    bind factor, link options
    turn seed

task get-stream
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <stream>
    turn seed

task set-stream
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <stream>
    bind factor, link value
