
task get-auto-gain-control
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <autoGainControl>
    turn seed

task set-auto-gain-control
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <autoGainControl>
    bind factor, link value

task get-width
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <width>
    turn seed

task set-width
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <width>
    bind factor, link value

task get-height
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <height>
    turn seed

task set-height
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <height>
    bind factor, link value

task get-aspect-ratio
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <aspectRatio>
    turn seed

task set-aspect-ratio
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <aspectRatio>
    bind factor, link value

task get-frame-rate
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <frameRate>
    turn seed

task set-frame-rate
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <frameRate>
    bind factor, link value

task get-facing-mode
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <facingMode>
    turn seed

task set-facing-mode
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <facingMode>
    bind factor, link value

task get-resize-mode
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <resizeMode>
    turn seed

task set-resize-mode
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <resizeMode>
    bind factor, link value

task get-volume
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <volume>
    turn seed

task set-volume
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <volume>
    bind factor, link value

task get-sample-rate
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <sampleRate>
    turn seed

task set-sample-rate
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <sampleRate>
    bind factor, link value

task get-sample-size
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <sampleSize>
    turn seed

task set-sample-size
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <sampleSize>
    bind factor, link value

task get-echo-cancellation
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <echoCancellation>
    turn seed

task set-echo-cancellation
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <echoCancellation>
    bind factor, link value

task get-latency
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <latency>
    turn seed

task set-latency
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <latency>
    bind factor, link value

task get-noise-suppression
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <noiseSuppression>
    turn seed

task set-noise-suppression
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <noiseSuppression>
    bind factor, link value

task get-channel-count
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <channelCount>
    turn seed

task set-channel-count
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <channelCount>
    bind factor, link value

task get-device-id
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <deviceId>
    turn seed

task set-device-id
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <deviceId>
    bind factor, link value

task get-group-id
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <groupId>
    turn seed

task set-group-id
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <groupId>
    bind factor, link value
