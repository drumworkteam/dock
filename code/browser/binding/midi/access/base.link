
task get-inputs
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <inputs>
    turn seed

task get-outputs
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <outputs>
    turn seed

task get-sysex-enabled
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <sysexEnabled>
    turn seed
