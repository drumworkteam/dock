
task get-amount-secure-context
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <amount Secure context>
    turn seed

task set-amount-secure-context
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <amount Secure context>
    bind factor, link value

task get-label-secure-context
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <label Secure context>
    turn seed

task set-label-secure-context
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <label Secure context>
    bind factor, link value

task get-pending-secure-context
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <pending Secure context>
    turn seed

task set-pending-secure-context
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <pending Secure context>
    bind factor, link value
