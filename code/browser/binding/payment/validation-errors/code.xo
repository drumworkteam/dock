
task get-error
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <error>
    turn seed

task set-error
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <error>
    bind factor, link value

task get-payer
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <payer>
    turn seed

task set-payer
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <payer>
    bind factor, link value

task get-payment-method
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <paymentMethod>
    turn seed

task set-payment-method
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <paymentMethod>
    bind factor, link value

task get-shipping-address
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <shippingAddress>
    turn seed

task set-shipping-address
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <shippingAddress>
    bind factor, link value
