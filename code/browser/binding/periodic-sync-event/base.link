
task build-periodic-sync-event
  base type
  base periodic-sync-event-init-dict

  call make
    bind constructor, text <PeriodicSyncEvent>
    bind factor, link type
    bind factor, link periodic-sync-event-init-dict
    turn seed

task get-tag
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <tag>
    turn seed
