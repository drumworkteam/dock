
task build-promise-rejection-event
  base type
  base options

  call make
    bind constructor, text <PromiseRejectionEvent>
    bind factor, link type
    bind factor, link options
    turn seed

task get-promise
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <promise>
    turn seed

task get-reason
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <reason>
    turn seed
