
task build-push-event
  base type
  base event-init-dict

  call make
    bind constructor, text <PushEvent>
    bind factor, link type
    bind factor, link event-init-dict
    turn seed

task get-data
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <data>
    turn seed
