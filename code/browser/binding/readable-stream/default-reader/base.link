
task build-readable-stream-default-reader
  base stream

  call make
    bind constructor, text <ReadableStreamDefaultReader>
    bind factor, link stream
    turn seed

task get-closed
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <closed>
    turn seed

task cancel
  base build
  base reason

  cause call-base
    bind object, link build
    bind method, text <cancel>
    bind factor, link reason
    turn seed

task read
  base build

  cause call-base
    bind object, link build
    bind method, text <read>
    turn seed

task release-lock
  base build

  cause call-base
    bind object, link build
    bind method, text <releaseLock>
    turn seed
