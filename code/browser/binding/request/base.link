
task build-request
  base input
  base init

  call make
    bind constructor, text <Request>
    bind factor, link input
    bind factor, link init
    turn seed

task get-cache
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <cache>
    turn seed

task get-context
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <context>
    turn seed

task get-credentials
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <credentials>
    turn seed

task get-destination
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <destination>
    turn seed

task get-headers
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <headers>
    turn seed

task get-integrity
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <integrity>
    turn seed

task get-method
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <method>
    turn seed

task get-mode
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <mode>
    turn seed

task get-redirect
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <redirect>
    turn seed

task get-referrer
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <referrer>
    turn seed

task get-referrer-policy
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <referrerPolicy>
    turn seed

task get-url
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <url>
    turn seed

task clone
  base build

  cause call-base
    bind object, link build
    bind method, text <clone>
    turn seed
