
task build-rtc-error-event
  base tones
  base duration
  base inter-tone-gap

  call make
    bind constructor, text <RTCErrorEvent>
    bind factor, link tones
    bind factor, link duration
    bind factor, link inter-tone-gap
    turn seed

task get-error
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <error>
    turn seed
