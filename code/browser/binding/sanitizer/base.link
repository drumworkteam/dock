
task sanitize-to-string
  base build
  base input

  cause call-base
    bind object, link build
    bind method, text <sanitizeToString>
    bind factor, link input
    turn seed

task sanitize
  base build
  base input

  cause call-base
    bind object, link build
    bind method, text <sanitize>
    bind factor, link input
    turn seed
