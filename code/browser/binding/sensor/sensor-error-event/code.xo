
task build-sensor-error-event
  base type
  base options

  call make
    bind constructor, text <SensorErrorEvent>
    bind factor, link type
    bind factor, link options
    turn seed

task get-error
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <error>
    turn seed
