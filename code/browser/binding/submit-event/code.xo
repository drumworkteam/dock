
task build-submit-event
  base type
  base event-init-dict

  call make
    bind constructor, text <SubmitEvent>
    bind factor, link type
    bind factor, link event-init-dict
    turn seed

task get-submitter
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <submitter>
    turn seed
