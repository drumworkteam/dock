
task get-base-val
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <baseVal>
    turn seed

task get-anim-val
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <animVal>
    turn seed
