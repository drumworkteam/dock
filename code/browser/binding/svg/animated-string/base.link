
task get-anim-val
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <animVal>
    turn seed

task get-base-val
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <baseVal>
    turn seed

task set-base-val
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <baseVal>
    bind factor, link value
