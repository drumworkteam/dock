
task get-in1
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <in1>
    turn seed

task get-in2
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <in2>
    turn seed

task get-mode
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <mode>
    turn seed
