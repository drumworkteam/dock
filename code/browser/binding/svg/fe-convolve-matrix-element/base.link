
task get-in1
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <in1>
    turn seed

task get-order-x
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <orderX>
    turn seed

task get-order-y
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <orderY>
    turn seed

task get-kernel-matrix
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <kernelMatrix>
    turn seed

task get-divisor
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <divisor>
    turn seed

task get-bias
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <bias>
    turn seed

task get-target-x
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <targetX>
    turn seed

task get-target-y
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <targetY>
    turn seed

task get-edge-mode
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <edgeMode>
    turn seed

task get-kernel-unit-length-x
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <kernelUnitLengthX>
    turn seed

task get-kernel-unit-length-y
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <kernelUnitLengthY>
    turn seed

task get-preserve-alpha
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <preserveAlpha>
    turn seed
