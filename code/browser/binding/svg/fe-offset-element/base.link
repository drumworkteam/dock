
task get-in1
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <in1>
    turn seed

task get-dx
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <dx>
    turn seed

task get-dy
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <dy>
    turn seed
