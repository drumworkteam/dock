
task get-path-length
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <pathLength>
    turn seed

task set-path-length
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <pathLength>
    bind factor, link value
