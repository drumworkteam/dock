
task register
  base build
  base options

  cause call-base
    bind object, link build
    bind method, text <register>
    bind factor, link options

task get-tags
  base build

  cause call-base
    bind object, link build
    bind method, text <getTags>
