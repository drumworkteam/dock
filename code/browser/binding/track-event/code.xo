
task build-track-event
  base type
  base event-info

  call make
    bind constructor, text <TrackEvent>
    bind factor, link type
    bind factor, link event-info
    turn seed

task get-track
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <track>
    turn seed
