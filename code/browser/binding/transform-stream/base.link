
task build-transform-stream
  base type
  base event-info

  call make
    bind constructor, text <TransformStream>
    bind factor, link type
    bind factor, link event-info
    turn seed

task get-readable
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <readable>
    turn seed

task get-writable
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <writable>
    turn seed
