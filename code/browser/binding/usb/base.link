
task get-devices
  base build

  cause call-base
    bind object, link build
    bind method, text <getDevices>
    turn seed

task request-device
  base build
  base filters

  cause call-base
    bind object, link build
    bind method, text <requestDevice>
    bind factor, link filters
    turn seed
