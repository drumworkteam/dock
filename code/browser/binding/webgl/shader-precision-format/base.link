
task get-range-min
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <rangeMin>
    turn seed

task get-range-max
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <rangeMax>
    turn seed

task get-precision
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <precision>
    turn seed
