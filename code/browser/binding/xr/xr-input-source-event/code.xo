
task build-x-r-input-source-event
  base type
  base event-init-dict

  call make
    bind constructor, text <XRInputSourceEvent>
    bind factor, link type
    bind factor, link event-init-dict
    turn seed

task get-frame
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <frame>
    turn seed

task get-input-source
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <inputSource>
    turn seed
