
task get-transform
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <transform>
    turn seed

task get-emulated-position
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <emulatedPosition>
    turn seed
