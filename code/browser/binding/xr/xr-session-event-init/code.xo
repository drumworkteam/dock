
task get-session
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <session>
    turn seed

task set-session
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <session>
    bind factor, link value
