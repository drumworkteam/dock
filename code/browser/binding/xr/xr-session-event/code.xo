
task build-x-r-session-event
  base type
  base event-init-dict

  call make
    bind constructor, text <XRSessionEvent>
    bind factor, link type
    bind factor, link event-init-dict
    turn seed

task get-session
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <session>
    turn seed
