
task get-eye
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <eye>
    turn seed

task get-projection-matrix
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <projectionMatrix>
    turn seed

task get-transform
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <transform>
    turn seed
