
task build-blob-event
  call make
    bind constructor, text <BlobEvent>
    turn seed

task get-data
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <data>
    turn seed

task get-timecode
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <timecode>
    turn seed
