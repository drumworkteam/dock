
task build-composition-event
  call make
    bind constructor, text <CompositionEvent>
    turn seed

task get-data
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <data>
    turn seed

task get-locale
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <locale>
    turn seed
