
task add
  base build
  base content-description

  cause call-base
    bind object, link build
    bind method, text <add>
    bind factor, link content-description
    turn seed

task delete
  base build

  cause call-base
    bind object, link build
    bind method, text <delete>
    turn seed

task get-all
  base build

  cause call-base
    bind object, link build
    bind method, text <getAll>
    turn seed
