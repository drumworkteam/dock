
task build-document-timeline
  base options

  call make
    bind constructor, text <DocumentTimeline>
    bind factor, link options
    turn seed

task get-current-time
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <currentTime>
    turn seed

task set-current-time
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <currentTime>
    bind factor, link value
