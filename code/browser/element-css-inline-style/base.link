
task get-style
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <style>
    turn seed

task set-style
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <style>
    bind factor, link value
