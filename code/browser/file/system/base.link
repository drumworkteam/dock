
task get-name
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <name>
    turn seed

task get-root
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <root>
    turn seed
