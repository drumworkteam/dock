
task build-gamepad-event
  base type-arg
  base options

  call make
    bind constructor, text <GamepadEvent>
    bind factor, link type-arg
    bind factor, link options
    turn seed

task get-gamepad
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <gamepad>
    turn seed
