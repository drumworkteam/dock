
task build-media-key-message-event
  base type-arg
  base options

  call make
    bind constructor, text <MediaKeyMessageEvent>
    bind factor, link type-arg
    bind factor, link options
    turn seed

task get-message
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <message>
    turn seed

task get-message-type
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <messageType>
    turn seed
