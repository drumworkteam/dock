
task build-mutation-observer
  base callback

  call make
    bind constructor, text <MutationObserver>
    bind factor, link callback
    turn seed

task disconnect
  base build

  cause call-base
    bind object, link build
    bind method, text <disconnect>
    turn seed

task observe
  base build
  base target
  base options
  base options

  cause call-base
    bind object, link build
    bind method, text <observe>
    bind factor, link target
    bind factor, link options
    bind factor, link options
    turn seed

task take-records
  base build

  cause call-base
    bind object, link build
    bind method, text <takeRecords>
    turn seed
