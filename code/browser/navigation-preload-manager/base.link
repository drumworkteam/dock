
task enable
  base build

  cause call-base
    bind object, link build
    bind method, text <enable>

task disable
  base build

  cause call-base
    bind object, link build
    bind method, text <disable>

task set-header-value
  base build

  cause call-base
    bind object, link build
    bind method, text <setHeaderValue>

task get-state
  base build

  cause call-base
    bind object, link build
    bind method, text <getState>
