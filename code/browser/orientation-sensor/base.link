
task get-quaternion
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <quaternion>
    turn seed

task set-quaternion
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <quaternion>
    bind factor, link value

task populate-matrix
  base build
  base target-matrix

  cause call-base
    bind object, link build
    bind method, text <populateMatrix>
    bind factor, link target-matrix
    turn seed
