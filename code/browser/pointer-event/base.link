
task get-pointer-id
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <pointerId>
    turn seed

task get-width
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <width>
    turn seed

task get-height
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <height>
    turn seed

task get-pressure
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <pressure>
    turn seed

task get-tangential-pressure
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <tangentialPressure>
    turn seed

task get-tilt-x
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <tiltX>
    turn seed

task get-tilt-y
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <tiltY>
    turn seed

task get-twist
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <twist>
    turn seed

task get-pointer-type
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <pointerType>
    turn seed

task get-is-primary
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <isPrimary>
    turn seed
