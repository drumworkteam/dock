
task get-tone-buffer
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <toneBuffer>
    turn seed

task insert-d-t-m-f
  base build
  base tones
  base duration
  base inter-tone-gap

  cause call-base
    bind object, link build
    bind method, text <insertDTMF>
    bind factor, link tones
    bind factor, link duration
    bind factor, link inter-tone-gap
    turn seed
