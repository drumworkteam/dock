
task estimate-secure-context
  base build

  cause call-base
    bind object, link build
    bind method, text <estimate Secure context>
    turn seed

task persist-secure-context
  base build

  cause call-base
    bind object, link build
    bind method, text <persist Secure context>
    turn seed

task persisted-secure-context
  base build

  cause call-base
    bind object, link build
    bind method, text <persisted Secure context>
