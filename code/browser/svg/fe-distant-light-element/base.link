
task get-azimuth
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <azimuth>
    turn seed

task get-elevation
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <elevation>
    turn seed
