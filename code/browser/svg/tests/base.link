
task get-required-features
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <requiredFeatures>
    turn seed

task get-required-extensions
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <requiredExtensions>
    turn seed

task get-system-language
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <systemLanguage>
    turn seed
