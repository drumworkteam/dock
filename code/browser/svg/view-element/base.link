
task get-view-target
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <viewTarget>
    turn seed

task set-view-target
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <viewTarget>
    bind factor, link value
