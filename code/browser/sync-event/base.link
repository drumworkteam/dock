
task build-sync-event
  base type
  base init

  call make
    bind constructor, text <SyncEvent>
    bind factor, link type
    bind factor, link init
    turn seed

task get-tag
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <tag>
    turn seed

task get-last-chance
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <lastChance>
    turn seed
