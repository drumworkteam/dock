
task get-detail
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <detail>
    turn seed

task get-view
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <view>
    turn seed

task inittimeevent-domstring-typearg-abstractview-viewarg-long-detailarg
  base build
  base id

  cause call-base
    bind object, link build
    bind method, text <initTimeEvent(DOMString typeArg, AbstractView viewArg, long detailArg)>
    bind factor, link id
    turn seed
