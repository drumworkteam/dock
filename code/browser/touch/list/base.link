
task get-length
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <length>
    turn seed

task item
  base build
  base index

  cause call-base
    bind object, link build
    bind method, text <item>
    bind factor, link index
    turn seed
