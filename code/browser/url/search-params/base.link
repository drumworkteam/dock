
task build-url-search-params
  call make
    bind constructor, text <URLSearchParams>
    turn seed

task append
  base build
  base name
  base value

  cause call-base
    bind object, link build
    bind method, text <append>
    bind factor, link name
    bind factor, link value
    turn seed

task delete
  base build
  base name

  cause call-base
    bind object, link build
    bind method, text <delete>
    bind factor, link name
    turn seed

task entries
  base build

  cause call-base
    bind object, link build
    bind method, text <entries>
    turn seed

task for-each
  base build
  base callback

  cause call-base
    bind object, link build
    bind method, text <forEach>
    bind factor, link callback
    turn seed

task get
  base build
  base name

  cause call-base
    bind object, link build
    bind method, text <get>
    bind factor, link name
    turn seed

task get-all
  base build
  base name

  cause call-base
    bind object, link build
    bind method, text <getAll>
    bind factor, link name
    turn seed

task has
  base build
  base name

  cause call-base
    bind object, link build
    bind method, text <has>
    bind factor, link name
    turn seed

task keys
  base build

  cause call-base
    bind object, link build
    bind method, text <keys>
    turn seed

task set
  base build
  base name
  base value

  cause call-base
    bind object, link build
    bind method, text <set>
    bind factor, link name
    bind factor, link value
    turn seed

task sort
  base build

  cause call-base
    bind object, link build
    bind method, text <sort>
    turn seed

task to-string
  base build

  cause call-base
    bind object, link build
    bind method, text <toString>
    turn seed

task values
  base build

  cause call-base
    bind object, link build
    bind method, text <values>
    turn seed
