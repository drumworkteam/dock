
task build-vtt-region
  base start-time
  base end-time
  base text

  call make
    bind constructor, text <VTTRegion>
    bind factor, link start-time
    bind factor, link end-time
    bind factor, link text
    turn seed

task get-id
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <id>
    turn seed

task set-id
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <id>
    bind factor, link value

task get-width
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <width>
    turn seed

task set-width
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <width>
    bind factor, link value

task get-lines
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <lines>
    turn seed

task set-lines
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <lines>
    bind factor, link value

task get-region-anchor-x
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <regionAnchorX>
    turn seed

task set-region-anchor-x
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <regionAnchorX>
    bind factor, link value

task get-region-anchor-y
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <regionAnchorY>
    turn seed

task set-region-anchor-y
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <regionAnchorY>
    bind factor, link value

task get-viewport-anchor-x
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <viewportAnchorX>
    turn seed

task set-viewport-anchor-x
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <viewportAnchorX>
    bind factor, link value

task get-viewport-anchor-y
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <viewportAnchorY>
    turn seed

task set-viewport-anchor-y
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <viewportAnchorY>
    bind factor, link value

task get-scroll
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <scroll>
    turn seed

task set-scroll
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <scroll>
    bind factor, link value
