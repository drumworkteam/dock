
task get-released
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <released>
    turn seed

task get-type
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <type>
    turn seed

task release
  base build

  cause call-base
    bind object, link build
    bind method, text <release>
    turn seed
