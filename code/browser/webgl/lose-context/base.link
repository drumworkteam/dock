
task lose-context
  base build

  cause call-base
    bind object, link build
    bind method, text <loseContext>

task restore-context
  base build

  cause call-base
    bind object, link build
    bind method, text <restoreContext>
