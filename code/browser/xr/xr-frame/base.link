
task get-session
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <session>
    turn seed

task get-pose
  base build
  base space
  base base-space

  cause call-base
    bind object, link build
    bind method, text <getPose>
    bind factor, link space
    bind factor, link base-space
    turn seed

task get-viewer-pose
  base build
  base reference-space

  cause call-base
    bind object, link build
    bind method, text <getViewerPose>
    bind factor, link reference-space
    turn seed
