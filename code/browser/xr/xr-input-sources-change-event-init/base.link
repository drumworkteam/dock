
task get-added
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <added>
    turn seed

task get-removed
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <removed>
    turn seed

task get-session
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <session>
    turn seed
