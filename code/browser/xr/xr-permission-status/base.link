
task get-granted
  base build

  cause get-aspect
    bind object, link build
    bind aspect, text <granted>
    turn seed

task set-granted
  base build
  base value

  cause set-aspect
    bind object, link build
    bind aspect, text <granted>
    bind factor, link value
