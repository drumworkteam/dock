
task drive
  base check, form force
  base block, form force

  call loop
    bind check, link check
    bind block, link block

task check
  base check, form force
  base block, form force

  call test
    bind check, link check
    bind block, link block

task check-else
  base check, form force
  base block, form force
  base else, form force

  call test-else
    bind check, link check
    bind block, link block
    bind else, link else

task debug-function
  base function, form function

  call call-method
    bind object, <window>
    bind method, <debug>
    bind factor, link function

task debug
  call debug
    bind keyword, <debugger>

task queue
  base function, form function

  call call-method
    bind object, <window>
    bind method, <setImmediate>
    bind factor, link function

task compute-bitwise-or
  base left
  base right

  call call-binary-operation
    bind left, link left
    bind operation, <|>
    bind right, link right
    turn seed

task check-if-equal
  base left
  base right

  call call-binary-operation
    bind left, link left
    bind operation, <==>
    bind right, link right
    turn seed

task check-if-strictly-equal
  base left
  base right

  call call-binary-operation
    bind left, link left
    bind operation, <===>
    bind right, link right
    turn seed

task get-typeof
  base value

  call call-keyword
    bind keyword, <typeof>
    bind value, link value
    turn seed

task get-instanceof
  base left
  base right

  call call-keyword-2
    bind left, link left
    bind keyword, <instanceof>
    bind right, link right
    turn seed

task set-field
  base object
  base attribute, form chord
  base value

  call set-dynamic-aspect
    bind object, link object
    bind aspect, link attribute
    bind factor, link value

task get-field
  base object
  base attribute, form chord

  call get-dynamic-aspect
    bind object, link object
    bind aspect, link attribute
    turn seed

task remove-field
  base object
  base attribute, form chord

  call delete
    bind object, link object
    bind aspect, link attribute

task shift-left
  base left
  base right

  call call-binary-operation
    bind left, link left
    bind operation, <\<\<>
    bind right, link right
    turn seed

task shift-right
  base left
  base right

  call call-binary-operation
    bind left, link left
    bind operation, <\>\>>
    bind right, link right
    turn seed

task shift-right-unsigned
  base left
  base right

  call call-binary-operation
    bind left, link left
    bind operation, <\>\>\>>
    bind right, link right
    turn seed

task compute-bitwise-and
  base left
  base right

  call call-binary-operation
    bind left, link left
    bind operation, <&>
    bind right, link right
    turn seed

task check-or
  base left
  base right

  call call-binary-operation
    bind left, link left
    bind operation, <||>
    bind right, link right
    turn seed

task try-catch
  base block, form function
  base error, form function

  call call-try
    bind block, link block
    bind error, link error

task check-if-truthy
  base value

  call call-unary-operation
    bind operation, <!!>
    bind value, link value
    turn seed

task check-opposite
  base value

  call call-unary-operation
    bind operation, <!>
    bind value, link value
    turn seed

task check-not-equal
  base left
  base right

  call call-binary-operation
    bind left, link left
    bind operation, <!==>
    bind right, link right
    turn seed

task flip-block
  base value

  call call-unary-operation
    bind operation, <~>
    bind value, link value
    turn seed

task check-gt
  base left
  base right

  call call-binary-operation
    bind left, link left
    bind operation, <\>>
    bind right, link right
    turn seed

task check-lt
  base left
  base right

  call call-binary-operation
    bind left, link left
    bind operation, <\<>
    bind right, link right
    turn seed

task check-gte
  base left
  base right

  call call-binary-operation
    bind left, link left
    bind operation, <\>=>
    bind right, link right
    turn seed

task check-lte
  base left
  base right

  call call-binary-operation
    bind left, link left
    bind operation, <\<=>
    bind right, link right
    turn seed

task check-and
  base left
  base right

  call call-binary-operation
    bind left, link left
    bind operation, <&&>
    bind right, link right
    turn seed
