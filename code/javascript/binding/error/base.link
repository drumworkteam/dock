
fetch ../string
  catch form string

field error
  base message, form string

task get-stack
  base error, form error

  call get-aspect
    bind object, link error
    bind aspect, <stack>
    turn seed

task throw
  base message, form string

  call throw-error
    bind factor, link message
