
task create
  base string
  base options

  call invoke-constructor
    bind constructor, <RegExp>
    bind factor, link string
    bind factor, link options

task test
  base expression
  base string

  call call-base
    bind object, link expression
    bind method, <test>
    bind factor, link string
    raise return
