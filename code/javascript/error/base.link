
load ../string
  take form string

form error
  link message, form string

task create
  link message

  call make
    bind constructor, text <Error>
    bind factor, link message

task get-stack
  link error, form error

  call get-aspect
    bind object, link error
    bind aspect, text <stack>

task throw
  link message, form string

  call throw-error
    bind factor, link message
