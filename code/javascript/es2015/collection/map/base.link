
load @treesurf/moon
  take suit any
  take suit like
  take suit or
  take suit task
  take suit tuple

load @treesurf/nest/code/javascript/native
  take face native-boolean
  take face native-null
  take face native-number
  take face native-this
  take face native-undefined
  take face native-void

load @treesurf/nest/code/javascript
  take face array

load @treesurf/nest/code/javascript/es2015/collection/map
  take face map

face map, name <Map>
  head k
  head v

  link size, name <size>
    like native-number

  task clear, name <clear>
    free seed, like native-void

  task delete, name <delete>
    take key, like k
    free seed, like native-boolean

  task for-each, name <forEach>
    take callbackfn
      like task
        take value, like v
        take key, like k
        take map
          like map
            like k
            like v
        free seed, like native-void
    take this-arg, like any
      void true
    free seed, like native-void

  task get, name <get>
    take key, like k
    free seed
      like or
        like v
        like native-undefined

  task has, name <has>
    take key, like k
    free seed, like native-boolean

  task set, name <set>
    take key, like k
    take value, like v
    free seed, like native-this

face map
  hook self
    link prototype, name <prototype>
      like map
        like any
        like any

  hook make
    free seed
      like map
        like any
        like any

  hook make
    head k
    head v

    take entries
      like or
        like array
          like tuple
            like k
            like v
        like native-null
      void true
    free seed
      like map
        like k
        like v
