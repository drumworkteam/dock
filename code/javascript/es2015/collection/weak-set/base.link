
load @tunebond/moon
  take form like
  take form or

load @tunebond/bolt/code/javascript/native
  take form native-boolean
  take form native-null
  take form native-object
  take form native-this

load @tunebond/bolt/code/javascript
  take form array

load @tunebond/bolt/code/javascript/es2015/collection/weak-set
  take form weak-set

form weak-set, name <WeakSet>
  head t
    base native-object

  task add, name <add>
    like native-this

    take value, like t

  task delete, name <delete>
    like native-boolean

    take value, like t

  task has, name <has>
    like native-boolean

    take value, like t

form weak-set
  hook self
    link prototype, name <prototype>
      like weak-set
        like native-object

  hook make
    like weak-set
      like t

    head t
      base native-object
      back native-object

    take values
      like or
        like array
          like t
        like native-null
      void true
