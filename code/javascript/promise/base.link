
load @drumwork/seed/code/form/task
  take form task

form promise

task make
  link hook, form task

  call make
    bind constructor, text <Promise>
    bind factor, link hook

task call-all
  link array, list promise

  call call-base
    bind object, text <Promise>
    bind method, text <all>
    bind factor, link array
