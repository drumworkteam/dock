
task write-file
  link fs
  link path
  link content
  link encoding, text <utf-8>
  link callback, form function

  call call-base
    bind object, link fs
    bind method, text <writeFile>
    bind factor, link path
    bind factor, link content
    bind factor, link encoding
    bind factor, link callback

task read-file
  link fs
  link path
  link encoding, text <utf-8>
  link callback, form function

  call call-base
    bind object, link fs
    bind method, text <readFile>
    bind factor, link path
    bind factor, link encoding
    bind factor, link callback

task append-file
  link fs
  link path, form string
  link data, form string
  link callback, form function

  call call-base
    bind object, link fs
    bind method, text <appendFile>
    bind factor, link path
    bind factor, link data
    bind factor, link callback

task change-mode
  link fs
  link path, form string
  link mode
  link callback

  call call-base
    bind object, link fs
    bind method, text <chmod>
    bind factor, link path
    bind factor, link mode
    bind factor, link callback

task change-owner
  link fs
  link path
  link uid
  link gid
  link callback

  call call-base
    bind object, link fs
    bind method, text <chown>
    bind factor, link path
    bind factor, link uid
    bind factor, link gid
    bind factor, link callback

task copy-file
  link fs
  link src, form string
  link dest, form string
  link mode, form string
  link callback, form function

  call call-base
    bind object, link fs
    bind method, text <copyFile>
    bind factor, link src
    bind factor, link dest
    bind factor, link mode
    bind factor, link callback

task create-read-stream
  link fs
  link path, form string
  link options

  call call-base
    bind object, link fs
    bind method, text <createReadStream>
    bind factor, link path
    bind factor, link options
    raise build

task create-write-stream
  link fs
  link path, form string
  link options

  call call-base
    bind object, link fs
    bind method, text <createWriteStream>
    bind factor, link path
    bind factor, link options
    raise build

task build-directory
  link fs
  link path, form string
  link options
  link callback, form function

  call call-base
    bind object, link fs
    bind method, text <mkdir>
    bind factor, link path
    bind factor, link options
    bind factor, link callback

task read-directory
  link fs
  link path, form string
  link options
  link callback, form function

  call call-base
    bind object, link fs
    bind method, text <readdir>
    bind factor, link path
    bind factor, link options
    bind factor, link callback

task rename
  link fs
  link old-path, form string
  link new-path, form string
  link callback, form function

  call call-base
    bind object, link fs
    bind method, text <rename>
    bind factor, link old-path
    bind factor, link new-path
    bind factor, link callback

task clear-directory
  link fs
  link path, form string
  link options
  link callback, form function

  call call-base
    bind object, link fs
    bind method, text <rmdir>
    bind factor, link path
    bind factor, link options
    bind factor, link callback

task stat
  link fs
  link path, form string
  link options
  link callback, form function

  call call-base
    bind object, link fs
    bind method, text <stat>
    bind factor, link path
    bind factor, link options
    bind factor, link callback

task truncate
  link fs
  link path, form string
  link size, form unsigned-integer
  link callback, form function

  call call-base
    bind object, link fs
    bind method, text <truncate>
    bind factor, link path
    bind factor, link size
    bind factor, link callback

task unlink
  link fs
  link path, form string
  link callback, form function

  call call-base
    bind object, link fs
    bind method, text <unlink>
    bind factor, link path
    bind factor, link callback
