
form isize, name <isize>
  task from-str-radix, name <from_str_radix>
    note <Converts a string slice in a given base to an integer.>

    take src
      like str
    take radix
      like u32
        cite free
      cite free

    free seed
      like result
        like isize
          cite free
        like parse-int-error
          cite free
        cite free

  task count-ones, name <count_ones>
    note <Returns the number of ones in the binary representation of self.>

    take self
      cite free

    free seed
      like u32
        cite free

  task count-zeros, name <count_zeros>
    note <Returns the number of zeros in the binary representation of self.>

    take self
      cite free

    free seed
      like u32
        cite free

  task leading-zeros, name <leading_zeros>
    note <Returns the number of leading zeros in the binary representation of self.>

    take self
      cite free

    free seed
      like u32
        cite free

  task trailing-zeros, name <trailing_zeros>
    note <Returns the number of trailing zeros in the binary representation of self.>

    take self
      cite free

    free seed
      like u32
        cite free

  task leading-ones, name <leading_ones>
    note <Returns the number of leading ones in the binary representation of self.>

    take self
      cite free

    free seed
      like u32
        cite free

  task trailing-ones, name <trailing_ones>
    note <Returns the number of trailing ones in the binary representation of self.>

    take self
      cite free

    free seed
      like u32
        cite free

  task rotate-left, name <rotate_left>
    note <Shifts the bits to the left by a specified amount, n, wrapping the truncated bits to the end of the resulting integer.>

    take self
      cite free
    take n
      like u32
        cite free
      cite free

    free seed
      like isize
        cite free

  task rotate-right, name <rotate_right>
    note <Shifts the bits to the right by a specified amount, n, wrapping the truncated bits to the beginning of the resulting integer.>

    take self
      cite free
    take n
      like u32
        cite free
      cite free

    free seed
      like isize
        cite free

  task swap-bytes, name <swap_bytes>
    note <Reverses the byte order of the integer.>

    take self
      cite free

    free seed
      like isize
        cite free

  task reverse-bits, name <reverse_bits>
    note <Reverses the order of bits in the integer. The least significant bit becomes the most significant bit, second least-significant bit becomes second most-significant bit, etc.>

    take self
      cite free

    free seed
      like isize
        cite free

  task from-be, name <from_be>
    note <Converts an integer from big endian to the target's endianness.>

    take x
      like isize
        cite free
      cite free

    free seed
      like isize
        cite free

  task from-le, name <from_le>
    note <Converts an integer from little endian to the target's endianness.>

    take x
      like isize
        cite free
      cite free

    free seed
      like isize
        cite free

  task to-be, name <to_be>
    note <Converts self to big endian from the target's endianness.>

    take self
      cite free

    free seed
      like isize
        cite free

  task to-le, name <to_le>
    note <Converts self to little endian from the target's endianness.>

    take self
      cite free

    free seed
      like isize
        cite free

  task checked-add, name <checked_add>
    note <Checked integer addition. Computes self + rhs, returning None if overflow occurred.>

    take self
      cite free
    take rhs
      like isize
        cite free
      cite free

    free seed
      like option
        like isize
          cite free
        cite free

  task unchecked-add, name <unchecked_add>
    note <Unchecked integer addition. Computes self + rhs, assuming overflow cannot occur.>

    take self
      cite free
    take rhs
      like isize
        cite free
      cite free

    free seed
      like isize
        cite free

  task checked-add-unsigned, name <checked_add_unsigned>
    note <Checked addition with an unsigned integer. Computes self + rhs, returning None if overflow occurred.>

    take self
      cite free
    take rhs
      like usize
        cite free
      cite free

    free seed
      like option
        like isize
          cite free
        cite free

  task checked-sub, name <checked_sub>
    note <Checked integer subtraction. Computes self - rhs, returning None if overflow occurred.>

    take self
      cite free
    take rhs
      like isize
        cite free
      cite free

    free seed
      like option
        like isize
          cite free
        cite free

  task unchecked-sub, name <unchecked_sub>
    note <Unchecked integer subtraction. Computes self - rhs, assuming overflow cannot occur.>

    take self
      cite free
    take rhs
      like isize
        cite free
      cite free

    free seed
      like isize
        cite free

  task checked-sub-unsigned, name <checked_sub_unsigned>
    note <Checked subtraction with an unsigned integer. Computes self - rhs, returning None if overflow occurred.>

    take self
      cite free
    take rhs
      like usize
        cite free
      cite free

    free seed
      like option
        like isize
          cite free
        cite free

  task checked-mul, name <checked_mul>
    note <Checked integer multiplication. Computes self * rhs, returning None if overflow occurred.>

    take self
      cite free
    take rhs
      like isize
        cite free
      cite free

    free seed
      like option
        like isize
          cite free
        cite free

  task unchecked-mul, name <unchecked_mul>
    note <Unchecked integer multiplication. Computes self * rhs, assuming overflow cannot occur.>

    take self
      cite free
    take rhs
      like isize
        cite free
      cite free

    free seed
      like isize
        cite free

  task checked-div, name <checked_div>
    note <Checked integer division. Computes self / rhs, returning None if rhs == 0 or the division results in overflow.>

    take self
      cite free
    take rhs
      like isize
        cite free
      cite free

    free seed
      like option
        like isize
          cite free
        cite free

  task checked-div-euclid, name <checked_div_euclid>
    note <Checked Euclidean division. Computes self.div_euclid(rhs), returning None if rhs == 0 or the division results in overflow.>

    take self
      cite free
    take rhs
      like isize
        cite free
      cite free

    free seed
      like option
        like isize
          cite free
        cite free

  task checked-rem, name <checked_rem>
    note <Checked integer remainder. Computes self % rhs, returning None if rhs == 0 or the division results in overflow.>

    take self
      cite free
    take rhs
      like isize
        cite free
      cite free

    free seed
      like option
        like isize
          cite free
        cite free

  task checked-rem-euclid, name <checked_rem_euclid>
    note <Checked Euclidean remainder. Computes self.rem_euclid(rhs), returning None if rhs == 0 or the division results in overflow.>

    take self
      cite free
    take rhs
      like isize
        cite free
      cite free

    free seed
      like option
        like isize
          cite free
        cite free

  task checked-neg, name <checked_neg>
    note <Checked negation. Computes -self, returning None if self == MIN.>

    take self
      cite free

    free seed
      like option
        like isize
          cite free
        cite free

  task checked-shl, name <checked_shl>
    note <Checked shift left. Computes self &lt;&lt; rhs, returning None if rhs is larger than or equal to the number of bits in self.>

    take self
      cite free
    take rhs
      like u32
        cite free
      cite free

    free seed
      like option
        like isize
          cite free
        cite free

  task unchecked-shl, name <unchecked_shl>
    note <Unchecked shift left. Computes self &lt;&lt; rhs, assuming that rhs is less than the number of bits in self.>

    take self
      cite free
    take rhs
      like isize
        cite free
      cite free

    free seed
      like isize
        cite free

  task checked-shr, name <checked_shr>
    note <Checked shift right. Computes self &gt;&gt; rhs, returning None if rhs is larger than or equal to the number of bits in self.>

    take self
      cite free
    take rhs
      like u32
        cite free
      cite free

    free seed
      like option
        like isize
          cite free
        cite free

  task unchecked-shr, name <unchecked_shr>
    note <Unchecked shift right. Computes self &gt;&gt; rhs, assuming that rhs is less than the number of bits in self.>

    take self
      cite free
    take rhs
      like isize
        cite free
      cite free

    free seed
      like isize
        cite free

  task checked-abs, name <checked_abs>
    note <Checked absolute value. Computes self.abs(), returning None if self == MIN.>

    take self
      cite free

    free seed
      like option
        like isize
          cite free
        cite free

  task checked-pow, name <checked_pow>
    note <Checked exponentiation. Computes self.pow(exp), returning None if overflow occurred.>

    take self
      cite free
    take exp
      like u32
        cite free
      cite free

    free seed
      like option
        like isize
          cite free
        cite free

  task saturating-add, name <saturating_add>
    note <Saturating integer addition. Computes self + rhs, saturating at the numeric bounds instead of overflowing.>

    take self
      cite free
    take rhs
      like isize
        cite free
      cite free

    free seed
      like isize
        cite free

  task saturating-add-unsigned, name <saturating_add_unsigned>
    note <Saturating addition with an unsigned integer. Computes self + rhs, saturating at the numeric bounds instead of overflowing.>

    take self
      cite free
    take rhs
      like usize
        cite free
      cite free

    free seed
      like isize
        cite free

  task saturating-sub, name <saturating_sub>
    note <Saturating integer subtraction. Computes self - rhs, saturating at the numeric bounds instead of overflowing.>

    take self
      cite free
    take rhs
      like isize
        cite free
      cite free

    free seed
      like isize
        cite free

  task saturating-sub-unsigned, name <saturating_sub_unsigned>
    note <Saturating subtraction with an unsigned integer. Computes self - rhs, saturating at the numeric bounds instead of overflowing.>

    take self
      cite free
    take rhs
      like usize
        cite free
      cite free

    free seed
      like isize
        cite free

  task saturating-neg, name <saturating_neg>
    note <Saturating integer negation. Computes -self, returning MAX if self == MIN instead of overflowing.>

    take self
      cite free

    free seed
      like isize
        cite free

  task saturating-abs, name <saturating_abs>
    note <Saturating absolute value. Computes self.abs(), returning MAX if self == MIN instead of overflowing.>

    take self
      cite free

    free seed
      like isize
        cite free

  task saturating-mul, name <saturating_mul>
    note <Saturating integer multiplication. Computes self * rhs, saturating at the numeric bounds instead of overflowing.>

    take self
      cite free
    take rhs
      like isize
        cite free
      cite free

    free seed
      like isize
        cite free

  task saturating-div, name <saturating_div>
    note <Saturating integer division. Computes self / rhs, saturating at the numeric bounds instead of overflowing.>

    take self
      cite free
    take rhs
      like isize
        cite free
      cite free

    free seed
      like isize
        cite free

  task saturating-pow, name <saturating_pow>
    note <Saturating integer exponentiation. Computes self.pow(exp), saturating at the numeric bounds instead of overflowing.>

    take self
      cite free
    take exp
      like u32
        cite free
      cite free

    free seed
      like isize
        cite free

  task wrapping-add, name <wrapping_add>
    note <Wrapping (modular) addition. Computes self + rhs, wrapping around at the boundary of the type.>

    take self
      cite free
    take rhs
      like isize
        cite free
      cite free

    free seed
      like isize
        cite free

  task wrapping-add-unsigned, name <wrapping_add_unsigned>
    note <Wrapping (modular) addition with an unsigned integer. Computes self + rhs, wrapping around at the boundary of the type.>

    take self
      cite free
    take rhs
      like usize
        cite free
      cite free

    free seed
      like isize
        cite free

  task wrapping-sub, name <wrapping_sub>
    note <Wrapping (modular) subtraction. Computes self - rhs, wrapping around at the boundary of the type.>

    take self
      cite free
    take rhs
      like isize
        cite free
      cite free

    free seed
      like isize
        cite free

  task wrapping-sub-unsigned, name <wrapping_sub_unsigned>
    note <Wrapping (modular) subtraction with an unsigned integer. Computes self - rhs, wrapping around at the boundary of the type.>

    take self
      cite free
    take rhs
      like usize
        cite free
      cite free

    free seed
      like isize
        cite free

  task wrapping-mul, name <wrapping_mul>
    note <Wrapping (modular) multiplication. Computes self * rhs, wrapping around at the boundary of the type.>

    take self
      cite free
    take rhs
      like isize
        cite free
      cite free

    free seed
      like isize
        cite free

  task wrapping-div, name <wrapping_div>
    note <Wrapping (modular) division. Computes self / rhs, wrapping around at the boundary of the type.>

    take self
      cite free
    take rhs
      like isize
        cite free
      cite free

    free seed
      like isize
        cite free

  task wrapping-div-euclid, name <wrapping_div_euclid>
    note <Wrapping Euclidean division. Computes self.div_euclid(rhs), wrapping around at the boundary of the type.>

    take self
      cite free
    take rhs
      like isize
        cite free
      cite free

    free seed
      like isize
        cite free

  task wrapping-rem, name <wrapping_rem>
    note <Wrapping (modular) remainder. Computes self % rhs, wrapping around at the boundary of the type.>

    take self
      cite free
    take rhs
      like isize
        cite free
      cite free

    free seed
      like isize
        cite free

  task wrapping-rem-euclid, name <wrapping_rem_euclid>
    note <Wrapping Euclidean remainder. Computes self.rem_euclid(rhs), wrapping around at the boundary of the type.>

    take self
      cite free
    take rhs
      like isize
        cite free
      cite free

    free seed
      like isize
        cite free

  task wrapping-neg, name <wrapping_neg>
    note <Wrapping (modular) negation. Computes -self, wrapping around at the boundary of the type.>

    take self
      cite free

    free seed
      like isize
        cite free

  task wrapping-shl, name <wrapping_shl>
    note <Panic-free bitwise shift-left; yields self &lt;&lt; mask(rhs), where mask removes any high-order bits of rhs that would cause the shift to exceed the bitwidth of the type.>

    take self
      cite free
    take rhs
      like u32
        cite free
      cite free

    free seed
      like isize
        cite free

  task wrapping-shr, name <wrapping_shr>
    note <Panic-free bitwise shift-right; yields self &gt;&gt; mask(rhs), where mask removes any high-order bits of rhs that would cause the shift to exceed the bitwidth of the type.>

    take self
      cite free
    take rhs
      like u32
        cite free
      cite free

    free seed
      like isize
        cite free

  task wrapping-abs, name <wrapping_abs>
    note <Wrapping (modular) absolute value. Computes self.abs(), wrapping around at the boundary of the type.>

    take self
      cite free

    free seed
      like isize
        cite free

  task unsigned-abs, name <unsigned_abs>
    note <Computes the absolute value of self without any wrapping or panicking.>

    take self
      cite free

    free seed
      like usize
        cite free

  task wrapping-pow, name <wrapping_pow>
    note <Wrapping (modular) exponentiation. Computes self.pow(exp), wrapping around at the boundary of the type.>

    take self
      cite free
    take exp
      like u32
        cite free
      cite free

    free seed
      like isize
        cite free

  task overflowing-add, name <overflowing_add>
    note <Calculates self + rhs>

    take self
      cite free
    take rhs
      like isize
        cite free
      cite free

    free seed
      like form
        take t-1, like isize
        take t-2, like bool

  task overflowing-add-unsigned, name <overflowing_add_unsigned>
    note <Calculates self + rhs with an unsigned rhs>

    take self
      cite free
    take rhs
      like usize
        cite free
      cite free

    free seed
      like form
        take t-1, like isize
        take t-2, like bool

  task overflowing-sub, name <overflowing_sub>
    note <Calculates self - rhs>

    take self
      cite free
    take rhs
      like isize
        cite free
      cite free

    free seed
      like form
        take t-1, like isize
        take t-2, like bool

  task overflowing-sub-unsigned, name <overflowing_sub_unsigned>
    note <Calculates self - rhs with an unsigned rhs>

    take self
      cite free
    take rhs
      like usize
        cite free
      cite free

    free seed
      like form
        take t-1, like isize
        take t-2, like bool

  task overflowing-mul, name <overflowing_mul>
    note <Calculates the multiplication of self and rhs.>

    take self
      cite free
    take rhs
      like isize
        cite free
      cite free

    free seed
      like form
        take t-1, like isize
        take t-2, like bool

  task overflowing-div, name <overflowing_div>
    note <Calculates the divisor when self is divided by rhs.>

    take self
      cite free
    take rhs
      like isize
        cite free
      cite free

    free seed
      like form
        take t-1, like isize
        take t-2, like bool

  task overflowing-div-euclid, name <overflowing_div_euclid>
    note <Calculates the quotient of Euclidean division self.div_euclid(rhs).>

    take self
      cite free
    take rhs
      like isize
        cite free
      cite free

    free seed
      like form
        take t-1, like isize
        take t-2, like bool

  task overflowing-rem, name <overflowing_rem>
    note <Calculates the remainder when self is divided by rhs.>

    take self
      cite free
    take rhs
      like isize
        cite free
      cite free

    free seed
      like form
        take t-1, like isize
        take t-2, like bool

  task overflowing-rem-euclid, name <overflowing_rem_euclid>
    note <Overflowing Euclidean remainder. Calculates self.rem_euclid(rhs).>

    take self
      cite free
    take rhs
      like isize
        cite free
      cite free

    free seed
      like form
        take t-1, like isize
        take t-2, like bool

  task overflowing-neg, name <overflowing_neg>
    note <Negates self, overflowing if this is equal to the minimum value.>

    take self
      cite free

    free seed
      like form
        take t-1, like isize
        take t-2, like bool

  task overflowing-shl, name <overflowing_shl>
    note <Shifts self left by rhs bits.>

    take self
      cite free
    take rhs
      like u32
        cite free
      cite free

    free seed
      like form
        take t-1, like isize
        take t-2, like bool

  task overflowing-shr, name <overflowing_shr>
    note <Shifts self right by rhs bits.>

    take self
      cite free
    take rhs
      like u32
        cite free
      cite free

    free seed
      like form
        take t-1, like isize
        take t-2, like bool

  task overflowing-abs, name <overflowing_abs>
    note <Computes the absolute value of self.>

    take self
      cite free

    free seed
      like form
        take t-1, like isize
        take t-2, like bool

  task overflowing-pow, name <overflowing_pow>
    note <Raises self to the power of exp, using exponentiation by squaring.>

    take self
      cite free
    take exp
      like u32
        cite free
      cite free

    free seed
      like form
        take t-1, like isize
        take t-2, like bool

  task pow, name <pow>
    note <Raises self to the power of exp, using exponentiation by squaring.>

    take self
      cite free
    take exp
      like u32
        cite free
      cite free

    free seed
      like isize
        cite free

  task div-euclid, name <div_euclid>
    note <Calculates the quotient of Euclidean division of self by rhs.>

    take self
      cite free
    take rhs
      like isize
        cite free
      cite free

    free seed
      like isize
        cite free

  task rem-euclid, name <rem_euclid>
    note <Calculates the least nonnegative remainder of self (mod rhs).>

    take self
      cite free
    take rhs
      like isize
        cite free
      cite free

    free seed
      like isize
        cite free

  task div-floor, name <div_floor>
    note <Calculates the quotient of self and rhs, rounding the result towards negative infinity.>

    take self
      cite free
    take rhs
      like isize
        cite free
      cite free

    free seed
      like isize
        cite free

  task div-ceil, name <div_ceil>
    note <Calculates the quotient of self and rhs, rounding the result towards positive infinity.>

    take self
      cite free
    take rhs
      like isize
        cite free
      cite free

    free seed
      like isize
        cite free

  task next-multiple-of, name <next_multiple_of>
    note <If rhs is positive, calculates the smallest value greater than or equal to self that is a multiple of rhs. If rhs is negative, calculates the largest value less than or equal to self that is a multiple of rhs.>

    take self
      cite free
    take rhs
      like isize
        cite free
      cite free

    free seed
      like isize
        cite free

  task checked-next-multiple-of, name <checked_next_multiple_of>
    note <If rhs is positive, calculates the smallest value greater than or equal to self that is a multiple of rhs. If rhs is negative, calculates the largest value less than or equal to self that is a multiple of rhs. Returns None if rhs is zero or the operation would result in overflow.>

    take self
      cite free
    take rhs
      like isize
        cite free
      cite free

    free seed
      like option
        like isize
          cite free
        cite free

  task log, name <log>
    note <Returns the logarithm of the number with respect to an arbitrary base, rounded down.>

    take self
      cite free
    take base
      like isize
        cite free
      cite free

    free seed
      like u32
        cite free

  task log2, name <log2>
    note <Returns the base 2 logarithm of the number, rounded down.>

    take self
      cite free

    free seed
      like u32
        cite free

  task log10, name <log10>
    note <Returns the base 10 logarithm of the number, rounded down.>

    take self
      cite free

    free seed
      like u32
        cite free

  task checked-log, name <checked_log>
    note <Returns the logarithm of the number with respect to an arbitrary base, rounded down.>

    take self
      cite free
    take base
      like isize
        cite free
      cite free

    free seed
      like option
        like u32
          cite free
        cite free

  task checked-log2, name <checked_log2>
    note <Returns the base 2 logarithm of the number, rounded down.>

    take self
      cite free

    free seed
      like option
        like u32
          cite free
        cite free

  task checked-log10, name <checked_log10>
    note <Returns the base 10 logarithm of the number, rounded down.>

    take self
      cite free

    free seed
      like option
        like u32
          cite free
        cite free

  task abs, name <abs>
    note <Computes the absolute value of self.>

    take self
      cite free

    free seed
      like isize
        cite free

  task abs-diff, name <abs_diff>
    note <Computes the absolute difference between self and other.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like usize
        cite free

  task signum, name <signum>
    note <Returns a number representing sign of self.>

    take self
      cite free

    free seed
      like isize
        cite free

  task is-positive, name <is_positive>
    note <Returns true if self is positive and false if the number is zero or negative.>

    take self
      cite free

    free seed
      like bool
        cite free

  task is-negative, name <is_negative>
    note <Returns true if self is negative and false if the number is zero or positive.>

    take self
      cite free

    free seed
      like bool
        cite free

  task to-be-bytes, name <to_be_bytes>
    note <Return the memory representation of this integer as a byte array in big-endian (network) byte order.>

    take self
      cite free

    free seed
      like list
        like u8
        name 8

  task to-le-bytes, name <to_le_bytes>
    note <Return the memory representation of this integer as a byte array in little-endian byte order.>

    take self
      cite free

    free seed
      like list
        like u8
        name 8

  task to-ne-bytes, name <to_ne_bytes>
    note <Return the memory representation of this integer as a byte array in native byte order.>

    take self
      cite free

    free seed
      like list
        like u8
        name 8

  task from-be-bytes, name <from_be_bytes>
    note <Create an integer value from its representation as a byte array in big endian.>

    take bytes
      like list
        like u8
        name 8
      cite free

    free seed
      like isize
        cite free

  task from-le-bytes, name <from_le_bytes>
    note <Create an integer value from its representation as a byte array in little endian.>

    take bytes
      like list
        like u8
        name 8
      cite free

    free seed
      like isize
        cite free

  task from-ne-bytes, name <from_ne_bytes>
    note <Create an integer value from its memory representation as a byte array in native endianness.>

    take bytes
      like list
        like u8
        name 8
      cite free

    free seed
      like isize
        cite free

  task min-value, name <min_value>
    note <New code should prefer to use isize::MIN instead.>

    free seed
      like isize
        cite free

  task max-value, name <max_value>
    note <New code should prefer to use isize::MAX instead.>

    free seed
      like isize
        cite free

  task add, name <add>
    note <Performs the + operation.>

    cast c-1
      like isize
      like add
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task add, name <add>
    note <Performs the + operation.>

    cast c-1
      like isize
      like add
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task add, name <add>
    note <Performs the + operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like isize
        cite free

  task add, name <add>
    note <Performs the + operation.>

    cast c-1
      like isize
      like add
        like isize
          cite free

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like c-1/output

  task add-assign, name <add_assign>
    note <Performs the += operation.>

    take self
      cite free
    take other
      like isize

  task add-assign, name <add_assign>
    note <Performs the += operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

  task fmt, name <fmt>
    note <Formats the value using the given formatter.>

    take self
      cite free
    take f
      like formatter
        time mask
      lock free

    free seed
      like result
        like form
        like error
          cite free
        cite free

  task bitand, name <bitand>
    note <Performs the & operation.>

    cast c-1
      like isize
      like bit-and
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task bitand, name <bitand>
    note <Performs the & operation.>

    cast c-1
      like isize
      like bit-and
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task bitand, name <bitand>
    note <Performs the & operation.>

    cast c-1
      like isize
      like bit-and
        like isize
          cite free

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like c-1/output

  task bitand, name <bitand>
    note <Performs the & operation.>

    take self
      cite free
    take rhs
      like isize
        cite free
      cite free

    free seed
      like isize
        cite free

  task bitand-assign, name <bitand_assign>
    note <Performs the &= operation.>

    take self
      cite free
    take other
      like isize

  task bitand-assign, name <bitand_assign>
    note <Performs the &= operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

  task bitor, name <bitor>
    note <Performs the | operation.>

    cast c-1
      like isize
      like bit-or
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task bitor, name <bitor>
    note <Performs the | operation.>

    cast c-1
      like isize
      like bit-or
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task bitor, name <bitor>
    note <Performs the | operation.>

    cast c-1
      like isize
      like bit-or
        like non-zero-isize
          cite free

    take self
      cite free
    take rhs
      like non-zero-isize
        cite free
      cite free

    free seed
      like c-1/output

  task bitor, name <bitor>
    note <Performs the | operation.>

    cast c-1
      like isize
      like bit-or
        like isize
          cite free

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like c-1/output

  task bitor, name <bitor>
    note <Performs the | operation.>

    cast c-1
      like non-zero-isize
      like bit-or
        like isize
          cite free

    take self
      cite free
    take rhs
      like isize
        cite free
      cite free

    free seed
      like c-1/output

  task bitor, name <bitor>
    note <Performs the | operation.>

    take self
      cite free
    take rhs
      like isize
        cite free
      cite free

    free seed
      like isize
        cite free

  task bitor-assign, name <bitor_assign>
    note <Performs the |= operation.>

    take self
      cite free
    take other
      like isize

  task bitor-assign, name <bitor_assign>
    note <Performs the |= operation.>

    take self
      cite free
    take rhs
      like isize
        cite free
      cite free

  task bitor-assign, name <bitor_assign>
    note <Performs the |= operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

  task bitxor, name <bitxor>
    note <Performs the ^ operation.>

    cast c-1
      like isize
      like bit-xor
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task bitxor, name <bitxor>
    note <Performs the ^ operation.>

    cast c-1
      like isize
      like bit-xor
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task bitxor, name <bitxor>
    note <Performs the ^ operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like isize
        cite free

  task bitxor, name <bitxor>
    note <Performs the ^ operation.>

    cast c-1
      like isize
      like bit-xor
        like isize
          cite free

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like c-1/output

  task bitxor-assign, name <bitxor_assign>
    note <Performs the ^= operation.>

    take self
      cite free
    take other
      like isize

  task bitxor-assign, name <bitxor_assign>
    note <Performs the ^= operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

  task clone, name <clone>
    note <Returns a copy of the value.>

    take self
      cite free

    free seed
      like isize
        cite free

  task clone-from, name <clone_from>
    note <Performs copy-assignment from source.>

    take self
      cite free
    take source
      like self

  task fmt, name <fmt>
    note <Formats the value using the given formatter.>

    take self
      cite free
    take f
      like formatter
        time mask
      lock free

    free seed
      like result
        like form
        like error
          cite free
        cite free

  task default, name <default>
    note <Returns the default value of 0>

    free seed
      like isize
        cite free

  task fmt, name <fmt>
    note <Formats the value using the given formatter.>

    take self
      cite free
    take f
      like formatter
        time mask
      lock free

    free seed
      like result
        like form
        like error
          cite free
        cite free

  task div, name <div>
    note <Performs the / operation.>

    cast c-1
      like isize
      like div
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task div, name <div>
    note <Performs the / operation.>

    cast c-1
      like isize
      like div
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task div, name <div>
    note <Performs the / operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like isize
        cite free

  task div, name <div>
    note <Performs the / operation.>

    cast c-1
      like isize
      like div
        like isize
          cite free

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like c-1/output

  task div-assign, name <div_assign>
    note <Performs the /= operation.>

    take self
      cite free
    take other
      like isize

  task div-assign, name <div_assign>
    note <Performs the /= operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

  task from, name <from>
    note <Converts a NonZeroIsize into an isize>

    take nonzero
      like non-zero-isize
        cite free
      cite free

    free seed
      like isize
        cite free

  task from, name <from>
    note <Converts a bool to a isize. The resulting value is 0 for false and 1 for true values.>

    take small
      like bool
        cite free
      cite free

    free seed
      like isize
        cite free

  task from, name <from>
    note <Converts i16 to isize losslessly.>

    take small
      like i16
        cite free
      cite free

    free seed
      like isize
        cite free

  task from, name <from>
    note <Converts i8 to isize losslessly.>

    take small
      like i8
        cite free
      cite free

    free seed
      like isize
        cite free

  task from, name <from>
    note <Converts an isize into an AtomicIsize.>

    take v
      like isize
        cite free
      cite free

    free seed
      like atomic-isize
        cite free

  task from, name <from>
    note <Converts u8 to isize losslessly.>

    take small
      like u8
        cite free
      cite free

    free seed
      like isize
        cite free

  task from-str, name <from_str>
    note <Parses a string s to return a value of this type.>

    take src
      like str

    free seed
      like result
        like isize
          cite free
        like parse-int-error
          cite free
        cite free

  task hash, name <hash>
    note <Feeds this value into the given Hasher.>

    lead h
      like hasher
        cite free

    head h

    take self
      cite free
    take state
      like h
      lock free

  task hash-slice, name <hash_slice>
    note <Feeds a slice of this type into the given Hasher.>

    lead h
      like hasher
        cite free

    head h

    take data
      list isize
      cite free
    take state
      like h
      lock free

  task fmt, name <fmt>
    note <Formats the value using the given formatter.>

    take self
      cite free
    take f
      like formatter
        time mask
      lock free

    free seed
      like result
        like form
        like error
          cite free
        cite free

  task fmt, name <fmt>
    note <Formats the value using the given formatter.>

    take self
      cite free
    take f
      like formatter
        time mask
      lock free

    free seed
      like result
        like form
        like error
          cite free
        cite free

  task mul, name <mul>
    note <Performs the * operation.>

    cast c-1
      like isize
      like mul
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task mul, name <mul>
    note <Performs the * operation.>

    cast c-1
      like isize
      like mul
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task mul, name <mul>
    note <Performs the * operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like isize
        cite free

  task mul, name <mul>
    note <Performs the * operation.>

    cast c-1
      like isize
      like mul
        like isize
          cite free

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like c-1/output

  task mul-assign, name <mul_assign>
    note <Performs the *= operation.>

    take self
      cite free
    take other
      like isize

  task mul-assign, name <mul_assign>
    note <Performs the *= operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

  task neg, name <neg>
    note <Performs the unary - operation.>

    cast c-1
      like isize
      like neg

    take self
      cite free

    free seed
      like c-1/output

  task neg, name <neg>
    note <Performs the unary - operation.>

    take self
      cite free

    free seed
      like isize
        cite free

  task not, name <not>
    note <Performs the unary ! operation.>

    take self
      cite free

    free seed
      like isize
        cite free

  task not, name <not>
    note <Performs the unary ! operation.>

    cast c-1
      like isize
      like not

    take self
      cite free

    free seed
      like c-1/output

  task fmt, name <fmt>
    note <Formats the value using the given formatter.>

    take self
      cite free
    take f
      like formatter
        time mask
      lock free

    free seed
      like result
        like form
        like error
          cite free
        cite free

  task cmp, name <cmp>
    note <This method returns an Ordering between self and other.>

    take self
      cite free
    take other
      like isize

    free seed
      like ordering
        cite free

  task max, name <max>
    note <Compares and returns the maximum of two values.>

    take self
      cite free
    take other
      like self
        cite free
      cite free

    free seed
      like self
        cite free

  task min, name <min>
    note <Compares and returns the minimum of two values.>

    take self
      cite free
    take other
      like self
        cite free
      cite free

    free seed
      like self
        cite free

  task clamp, name <clamp>
    note <Restrict a value to a certain interval.>

    take self
      cite free
    take min
      like self
        cite free
      cite free
    take max
      like self
        cite free
      cite free

    free seed
      like self
        cite free

  task eq, name <eq>
    note <This method tests for self and other values to be equal, and is used by ==.>

    take self
      cite free
    take other
      like isize

    free seed
      like bool
        cite free

  task ne, name <ne>
    note <This method tests for !=.>

    take self
      cite free
    take other
      like isize

    free seed
      like bool
        cite free

  task partial-cmp, name <partial_cmp>
    note <This method returns an ordering between self and other values if one exists.>

    take self
      cite free
    take other
      like isize

    free seed
      like option
        like ordering
          cite free
        cite free

  task lt, name <lt>
    note <This method tests less than (for self and other) and is used by the &lt; operator.>

    take self
      cite free
    take other
      like isize

    free seed
      like bool
        cite free

  task le, name <le>
    note <This method tests less than or equal to (for self and other) and is used by the &lt;= operator.>

    take self
      cite free
    take other
      like isize

    free seed
      like bool
        cite free

  task ge, name <ge>
    note <This method tests greater than or equal to (for self and other) and is used by the &gt;= operator.>

    take self
      cite free
    take other
      like isize

    free seed
      like bool
        cite free

  task gt, name <gt>
    note <This method tests greater than (for self and other) and is used by the &gt; operator.>

    take self
      cite free
    take other
      like isize

    free seed
      like bool
        cite free

  task product, name <product>
    note <Method which takes an iterator and generates Self from the elements by multiplying the items.>

    lead i
      like iterator
        like item
          cite free
          fall back
            like isize
              time a
        cite free

    head i

    take iter
      like i
        cite free
      cite free

    free seed
      like isize
        cite free

  task product, name <product>
    note <Method which takes an iterator and generates Self from the elements by multiplying the items.>

    lead i
      like iterator
        like item
          cite free
          fall back
            like isize
              cite free
        cite free

    head i

    take iter
      like i
        cite free
      cite free

    free seed
      like isize
        cite free

  task rem, name <rem>
    note <Performs the % operation.>

    cast c-1
      like isize
      like rem
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task rem, name <rem>
    note <Performs the % operation.>

    cast c-1
      like isize
      like rem
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task rem, name <rem>
    note <Performs the % operation.>

    cast c-1
      like isize
      like rem
        like isize
          cite free

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like c-1/output

  task rem, name <rem>
    note <Performs the % operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like isize
        cite free

  task rem-assign, name <rem_assign>
    note <Performs the %= operation.>

    take self
      cite free
    take other
      like isize

  task rem-assign, name <rem_assign>
    note <Performs the %= operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like isize
      like shl
        like i128
          cite free

    take self
      cite free
    take other
      like i128

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like isize
      like shl
        like i128
          cite free

    take self
      cite free
    take other
      like i128

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like isize
      like shl
        like i16
          cite free

    take self
      cite free
    take other
      like i16

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like isize
      like shl
        like i16
          cite free

    take self
      cite free
    take other
      like i16

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like isize
      like shl
        like i32
          cite free

    take self
      cite free
    take other
      like i32

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like isize
      like shl
        like i32
          cite free

    take self
      cite free
    take other
      like i32

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like isize
      like shl
        like i64
          cite free

    take self
      cite free
    take other
      like i64

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like isize
      like shl
        like i64
          cite free

    take self
      cite free
    take other
      like i64

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like isize
      like shl
        like i8
          cite free

    take self
      cite free
    take other
      like i8

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like isize
      like shl
        like i8
          cite free

    take self
      cite free
    take other
      like i8

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like i16
      like shl
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like i128
      like shl
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like i64
      like shl
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like i64
      like shl
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like usize
      like shl
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like i8
      like shl
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u8
      like shl
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u16
      like shl
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like i32
      like shl
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like isize
      like shl
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like i32
      like shl
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like usize
      like shl
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u32
      like shl
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u8
      like shl
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u128
      like shl
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u128
      like shl
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u64
      like shl
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like i128
      like shl
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u16
      like shl
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like i8
      like shl
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u64
      like shl
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u32
      like shl
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like i16
      like shl
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like isize
      like shl
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like isize
      like shl
        like u128
          cite free

    take self
      cite free
    take other
      like u128

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like isize
      like shl
        like u128
          cite free

    take self
      cite free
    take other
      like u128

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like isize
      like shl
        like u16
          cite free

    take self
      cite free
    take other
      like u16

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like isize
      like shl
        like u16
          cite free

    take self
      cite free
    take other
      like u16

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like isize
      like shl
        like u32
          cite free

    take self
      cite free
    take other
      like u32

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like isize
      like shl
        like u32
          cite free

    take self
      cite free
    take other
      like u32

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like isize
      like shl
        like u64
          cite free

    take self
      cite free
    take other
      like u64

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like isize
      like shl
        like u64
          cite free

    take self
      cite free
    take other
      like u64

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like isize
      like shl
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like isize
      like shl
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like isize
      like shl
        like usize
          cite free

    take self
      cite free
    take other
      like usize

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like isize
      like shl
        like usize
          cite free

    take self
      cite free
    take other
      like usize

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    take self
      cite free
    take other
      like i128
        cite free
      cite free

    free seed
      like isize
        cite free

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like isize
      like shl
        like i128
          cite free

    take self
      cite free
    take other
      like i128
        cite free
      cite free

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    take self
      cite free
    take other
      like i16
        cite free
      cite free

    free seed
      like isize
        cite free

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like isize
      like shl
        like i16
          cite free

    take self
      cite free
    take other
      like i16
        cite free
      cite free

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    take self
      cite free
    take other
      like i32
        cite free
      cite free

    free seed
      like isize
        cite free

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like isize
      like shl
        like i32
          cite free

    take self
      cite free
    take other
      like i32
        cite free
      cite free

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like isize
      like shl
        like i64
          cite free

    take self
      cite free
    take other
      like i64
        cite free
      cite free

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    take self
      cite free
    take other
      like i64
        cite free
      cite free

    free seed
      like isize
        cite free

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    take self
      cite free
    take other
      like i8
        cite free
      cite free

    free seed
      like isize
        cite free

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like isize
      like shl
        like i8
          cite free

    take self
      cite free
    take other
      like i8
        cite free
      cite free

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like i128
        cite free

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like usize
        cite free

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u64
      like shl
        like isize
          cite free

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u128
      like shl
        like isize
          cite free

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like isize
        cite free

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like i64
        cite free

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like i16
        cite free

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u32
      like shl
        like isize
          cite free

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like i32
        cite free

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like i128
      like shl
        like isize
          cite free

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u8
      like shl
        like isize
          cite free

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like u128
        cite free

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u16
      like shl
        like isize
          cite free

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like i8
        cite free

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like usize
      like shl
        like isize
          cite free

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like i16
      like shl
        like isize
          cite free

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like i32
      like shl
        like isize
          cite free

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like u8
        cite free

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like u64
        cite free

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like isize
      like shl
        like isize
          cite free

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like u32
        cite free

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like u16
        cite free

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like i64
      like shl
        like isize
          cite free

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like i8
      like shl
        like isize
          cite free

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    take self
      cite free
    take other
      like u128
        cite free
      cite free

    free seed
      like isize
        cite free

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like isize
      like shl
        like u128
          cite free

    take self
      cite free
    take other
      like u128
        cite free
      cite free

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like isize
      like shl
        like u16
          cite free

    take self
      cite free
    take other
      like u16
        cite free
      cite free

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    take self
      cite free
    take other
      like u16
        cite free
      cite free

    free seed
      like isize
        cite free

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    take self
      cite free
    take other
      like u32
        cite free
      cite free

    free seed
      like isize
        cite free

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like isize
      like shl
        like u32
          cite free

    take self
      cite free
    take other
      like u32
        cite free
      cite free

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    take self
      cite free
    take other
      like u64
        cite free
      cite free

    free seed
      like isize
        cite free

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like isize
      like shl
        like u64
          cite free

    take self
      cite free
    take other
      like u64
        cite free
      cite free

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like isize
        cite free

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like isize
      like shl
        like u8
          cite free

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like isize
      like shl
        like usize
          cite free

    take self
      cite free
    take other
      like usize
        cite free
      cite free

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    take self
      cite free
    take other
      like usize
        cite free
      cite free

    free seed
      like isize
        cite free

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like i128

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like i16

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like i32

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like i64

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like i8

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like isize

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like isize

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like isize

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like isize

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like isize

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like isize

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like isize

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like isize

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like isize

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like isize

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like isize

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like isize

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like u128

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like u16

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like u32

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like u64

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like u8

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like usize

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like i128
        cite free
      cite free

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like i16
        cite free
      cite free

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like i32
        cite free
      cite free

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like i64
        cite free
      cite free

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like i8
        cite free
      cite free

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like u128
        cite free
      cite free

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like u16
        cite free
      cite free

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like u32
        cite free
      cite free

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like u64
        cite free
      cite free

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like usize
        cite free
      cite free

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like isize
      like shr
        like i128
          cite free

    take self
      cite free
    take other
      like i128

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like isize
      like shr
        like i128
          cite free

    take self
      cite free
    take other
      like i128

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like isize
      like shr
        like i16
          cite free

    take self
      cite free
    take other
      like i16

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like isize
      like shr
        like i16
          cite free

    take self
      cite free
    take other
      like i16

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like isize
      like shr
        like i32
          cite free

    take self
      cite free
    take other
      like i32

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like isize
      like shr
        like i32
          cite free

    take self
      cite free
    take other
      like i32

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like isize
      like shr
        like i64
          cite free

    take self
      cite free
    take other
      like i64

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like isize
      like shr
        like i64
          cite free

    take self
      cite free
    take other
      like i64

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like isize
      like shr
        like i8
          cite free

    take self
      cite free
    take other
      like i8

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like isize
      like shr
        like i8
          cite free

    take self
      cite free
    take other
      like i8

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like isize
      like shr
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like usize
      like shr
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like i8
      like shr
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like i128
      like shr
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u32
      like shr
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u8
      like shr
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u64
      like shr
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like i64
      like shr
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like usize
      like shr
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like i32
      like shr
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u8
      like shr
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like isize
      like shr
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like i64
      like shr
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u128
      like shr
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u64
      like shr
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like i32
      like shr
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u16
      like shr
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like i16
      like shr
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u16
      like shr
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like i16
      like shr
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u32
      like shr
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like i128
      like shr
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like i8
      like shr
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u128
      like shr
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like isize
      like shr
        like u128
          cite free

    take self
      cite free
    take other
      like u128

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like isize
      like shr
        like u128
          cite free

    take self
      cite free
    take other
      like u128

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like isize
      like shr
        like u16
          cite free

    take self
      cite free
    take other
      like u16

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like isize
      like shr
        like u16
          cite free

    take self
      cite free
    take other
      like u16

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like isize
      like shr
        like u32
          cite free

    take self
      cite free
    take other
      like u32

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like isize
      like shr
        like u32
          cite free

    take self
      cite free
    take other
      like u32

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like isize
      like shr
        like u64
          cite free

    take self
      cite free
    take other
      like u64

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like isize
      like shr
        like u64
          cite free

    take self
      cite free
    take other
      like u64

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like isize
      like shr
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like isize
      like shr
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like isize
      like shr
        like usize
          cite free

    take self
      cite free
    take other
      like usize

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like isize
      like shr
        like usize
          cite free

    take self
      cite free
    take other
      like usize

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    take self
      cite free
    take other
      like i128
        cite free
      cite free

    free seed
      like isize
        cite free

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like isize
      like shr
        like i128
          cite free

    take self
      cite free
    take other
      like i128
        cite free
      cite free

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like isize
      like shr
        like i16
          cite free

    take self
      cite free
    take other
      like i16
        cite free
      cite free

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    take self
      cite free
    take other
      like i16
        cite free
      cite free

    free seed
      like isize
        cite free

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    take self
      cite free
    take other
      like i32
        cite free
      cite free

    free seed
      like isize
        cite free

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like isize
      like shr
        like i32
          cite free

    take self
      cite free
    take other
      like i32
        cite free
      cite free

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like isize
      like shr
        like i64
          cite free

    take self
      cite free
    take other
      like i64
        cite free
      cite free

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    take self
      cite free
    take other
      like i64
        cite free
      cite free

    free seed
      like isize
        cite free

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like isize
      like shr
        like i8
          cite free

    take self
      cite free
    take other
      like i8
        cite free
      cite free

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    take self
      cite free
    take other
      like i8
        cite free
      cite free

    free seed
      like isize
        cite free

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like usize
      like shr
        like isize
          cite free

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like u32
        cite free

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u64
      like shr
        like isize
          cite free

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u8
      like shr
        like isize
          cite free

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u128
      like shr
        like isize
          cite free

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u16
      like shr
        like isize
          cite free

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like i8
      like shr
        like isize
          cite free

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like u8
        cite free

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like i16
      like shr
        like isize
          cite free

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like u64
        cite free

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like i32
        cite free

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like i64
      like shr
        like isize
          cite free

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like isize
        cite free

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like u128
        cite free

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like isize
      like shr
        like isize
          cite free

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like i128
      like shr
        like isize
          cite free

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like i64
        cite free

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u32
      like shr
        like isize
          cite free

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like i16
        cite free

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like i32
      like shr
        like isize
          cite free

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like u16
        cite free

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like i128
        cite free

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like i8
        cite free

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like usize
        cite free

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    take self
      cite free
    take other
      like u128
        cite free
      cite free

    free seed
      like isize
        cite free

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like isize
      like shr
        like u128
          cite free

    take self
      cite free
    take other
      like u128
        cite free
      cite free

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like isize
      like shr
        like u16
          cite free

    take self
      cite free
    take other
      like u16
        cite free
      cite free

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    take self
      cite free
    take other
      like u16
        cite free
      cite free

    free seed
      like isize
        cite free

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    take self
      cite free
    take other
      like u32
        cite free
      cite free

    free seed
      like isize
        cite free

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like isize
      like shr
        like u32
          cite free

    take self
      cite free
    take other
      like u32
        cite free
      cite free

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like isize
      like shr
        like u64
          cite free

    take self
      cite free
    take other
      like u64
        cite free
      cite free

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    take self
      cite free
    take other
      like u64
        cite free
      cite free

    free seed
      like isize
        cite free

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like isize
      like shr
        like u8
          cite free

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like isize
        cite free

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    take self
      cite free
    take other
      like usize
        cite free
      cite free

    free seed
      like isize
        cite free

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like isize
      like shr
        like usize
          cite free

    take self
      cite free
    take other
      like usize
        cite free
      cite free

    free seed
      like c-1/output

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like i128

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like i16

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like i32

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like i64

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like i8

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like isize

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like isize

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like isize

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like isize

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like isize

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like isize

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like isize

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like isize

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like isize

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like isize

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like isize

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like isize

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like u128

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like u16

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like u32

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like u64

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like u8

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like usize

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like i128
        cite free
      cite free

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like i16
        cite free
      cite free

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like i32
        cite free
      cite free

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like i64
        cite free
      cite free

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like i8
        cite free
      cite free

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like u128
        cite free
      cite free

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like u16
        cite free
      cite free

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like u32
        cite free
      cite free

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like u64
        cite free
      cite free

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like usize
        cite free
      cite free

  task forward-unchecked, name <forward_unchecked>
    note <Returns the value that would be obtained by taking the successor of self count times.>

    take start
      like isize
        cite free
      cite free
    take n
      like usize
        cite free
      cite free

    free seed
      like isize
        cite free

  task backward-unchecked, name <backward_unchecked>
    note <Returns the value that would be obtained by taking the predecessor of self count times.>

    take start
      like isize
        cite free
      cite free
    take n
      like usize
        cite free
      cite free

    free seed
      like isize
        cite free

  task forward, name <forward>
    note <Returns the value that would be obtained by taking the successor of self count times.>

    take start
      like isize
        cite free
      cite free
    take n
      like usize
        cite free
      cite free

    free seed
      like isize
        cite free

  task backward, name <backward>
    note <Returns the value that would be obtained by taking the predecessor of self count times.>

    take start
      like isize
        cite free
      cite free
    take n
      like usize
        cite free
      cite free

    free seed
      like isize
        cite free

  task steps-between, name <steps_between>
    note <Returns the number of successor steps required to get from start to end.>

    take start
      like isize
    take end
      like isize

    free seed
      like option
        like usize
          cite free
        cite free

  task forward-checked, name <forward_checked>
    note <Returns the value that would be obtained by taking the successor of self count times.>

    take start
      like isize
        cite free
      cite free
    take n
      like usize
        cite free
      cite free

    free seed
      like option
        like isize
          cite free
        cite free

  task backward-checked, name <backward_checked>
    note <Returns the value that would be obtained by taking the predecessor of self count times.>

    take start
      like isize
        cite free
      cite free
    take n
      like usize
        cite free
      cite free

    free seed
      like option
        like isize
          cite free
        cite free

  task sub, name <sub>
    note <Performs the - operation.>

    cast c-1
      like isize
      like sub
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task sub, name <sub>
    note <Performs the - operation.>

    cast c-1
      like isize
      like sub
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task sub, name <sub>
    note <Performs the - operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like isize
        cite free

  task sub, name <sub>
    note <Performs the - operation.>

    cast c-1
      like isize
      like sub
        like isize
          cite free

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like c-1/output

  task sub-assign, name <sub_assign>
    note <Performs the -= operation.>

    take self
      cite free
    take other
      like isize

  task sub-assign, name <sub_assign>
    note <Performs the -= operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

  task sum, name <sum>
    note <Method which takes an iterator and generates Self from the elements by “summing up” the items.>

    lead i
      like iterator
        like item
          cite free
          fall back
            like isize
              time a
        cite free

    head i

    take iter
      like i
        cite free
      cite free

    free seed
      like isize
        cite free

  task sum, name <sum>
    note <Method which takes an iterator and generates Self from the elements by “summing up” the items.>

    lead i
      like iterator
        like item
          cite free
          fall back
            like isize
              cite free
        cite free

    head i

    take iter
      like i
        cite free
      cite free

    free seed
      like isize
        cite free

  task try-from, name <try_from>
    note <Try to create the target number type from a source number type. This returns an error if the source value is outside of the range of the target type.>

    cast c-1
      like isize
      like try-from
        like i128
          cite free

    take u
      like i128
        cite free
      cite free

    free seed
      like result
        like isize
          cite free
        like c-1/error
        cite free

  task try-from, name <try_from>
    note <Try to create the target number type from a source number type. This returns an error if the source value is outside of the range of the target type.>

    cast c-1
      like isize
      like try-from
        like i32
          cite free

    take value
      like i32
        cite free
      cite free

    free seed
      like result
        like isize
          cite free
        like c-1/error
        cite free

  task try-from, name <try_from>
    note <Try to create the target number type from a source number type. This returns an error if the source value is outside of the range of the target type.>

    cast c-1
      like isize
      like try-from
        like i64
          cite free

    take value
      like i64
        cite free
      cite free

    free seed
      like result
        like isize
          cite free
        like c-1/error
        cite free

  task try-from, name <try_from>
    note <Try to create the target number type from a source number type. This returns an error if the source value is outside of the range of the target type.>

    cast c-1
      like i16
      like try-from
        like isize
          cite free

    take u
      like isize
        cite free
      cite free

    free seed
      like result
        like i16
          cite free
        like c-1/error
        cite free

  task try-from, name <try_from>
    note <Try to create the target number type from a source number type. This returns an error if the source value is outside of the range of the target type.>

    cast c-1
      like u64
      like try-from
        like isize
          cite free

    take u
      like isize
        cite free
      cite free

    free seed
      like result
        like u64
          cite free
        like c-1/error
        cite free

  task try-from, name <try_from>
    note <Try to create the target number type from a source number type. This returns an error if the source value is outside of the range of the target type.>

    cast c-1
      like i128
      like try-from
        like isize
          cite free

    take value
      like isize
        cite free
      cite free

    free seed
      like result
        like i128
          cite free
        like c-1/error
        cite free

  task try-from, name <try_from>
    note <Attempts to convert isize to NonZeroIsize.>

    cast c-1
      like non-zero-isize
      like try-from
        like isize
          cite free

    take value
      like isize
        cite free
      cite free

    free seed
      like result
        like non-zero-isize
          cite free
        like c-1/error
        cite free

  task try-from, name <try_from>
    note <Try to create the target number type from a source number type. This returns an error if the source value is outside of the range of the target type.>

    cast c-1
      like u16
      like try-from
        like isize
          cite free

    take u
      like isize
        cite free
      cite free

    free seed
      like result
        like u16
          cite free
        like c-1/error
        cite free

  task try-from, name <try_from>
    note <Try to create the target number type from a source number type. This returns an error if the source value is outside of the range of the target type.>

    cast c-1
      like u128
      like try-from
        like isize
          cite free

    take u
      like isize
        cite free
      cite free

    free seed
      like result
        like u128
          cite free
        like c-1/error
        cite free

  task try-from, name <try_from>
    note <Try to create the target number type from a source number type. This returns an error if the source value is outside of the range of the target type.>

    cast c-1
      like i32
      like try-from
        like isize
          cite free

    take u
      like isize
        cite free
      cite free

    free seed
      like result
        like i32
          cite free
        like c-1/error
        cite free

  task try-from, name <try_from>
    note <Try to create the target number type from a source number type. This returns an error if the source value is outside of the range of the target type.>

    cast c-1
      like i64
      like try-from
        like isize
          cite free

    take value
      like isize
        cite free
      cite free

    free seed
      like result
        like i64
          cite free
        like c-1/error
        cite free

  task try-from, name <try_from>
    note <Try to create the target number type from a source number type. This returns an error if the source value is outside of the range of the target type.>

    cast c-1
      like i8
      like try-from
        like isize
          cite free

    take u
      like isize
        cite free
      cite free

    free seed
      like result
        like i8
          cite free
        like c-1/error
        cite free

  task try-from, name <try_from>
    note <Try to create the target number type from a source number type. This returns an error if the source value is outside of the range of the target type.>

    cast c-1
      like usize
      like try-from
        like isize
          cite free

    take u
      like isize
        cite free
      cite free

    free seed
      like result
        like usize
          cite free
        like c-1/error
        cite free

  task try-from, name <try_from>
    note <Try to create the target number type from a source number type. This returns an error if the source value is outside of the range of the target type.>

    cast c-1
      like u32
      like try-from
        like isize
          cite free

    take u
      like isize
        cite free
      cite free

    free seed
      like result
        like u32
          cite free
        like c-1/error
        cite free

  task try-from, name <try_from>
    note <Try to create the target number type from a source number type. This returns an error if the source value is outside of the range of the target type.>

    cast c-1
      like u8
      like try-from
        like isize
          cite free

    take u
      like isize
        cite free
      cite free

    free seed
      like result
        like u8
          cite free
        like c-1/error
        cite free

  task try-from, name <try_from>
    note <Try to create the target number type from a source number type. This returns an error if the source value is outside of the range of the target type.>

    cast c-1
      like isize
      like try-from
        like u128
          cite free

    take u
      like u128
        cite free
      cite free

    free seed
      like result
        like isize
          cite free
        like c-1/error
        cite free

  task try-from, name <try_from>
    note <Try to create the target number type from a source number type. This returns an error if the source value is outside of the range of the target type.>

    cast c-1
      like isize
      like try-from
        like u16
          cite free

    take value
      like u16
        cite free
      cite free

    free seed
      like result
        like isize
          cite free
        like c-1/error
        cite free

  task try-from, name <try_from>
    note <Try to create the target number type from a source number type. This returns an error if the source value is outside of the range of the target type.>

    cast c-1
      like isize
      like try-from
        like u32
          cite free

    take value
      like u32
        cite free
      cite free

    free seed
      like result
        like isize
          cite free
        like c-1/error
        cite free

  task try-from, name <try_from>
    note <Try to create the target number type from a source number type. This returns an error if the source value is outside of the range of the target type.>

    cast c-1
      like isize
      like try-from
        like u64
          cite free

    take u
      like u64
        cite free
      cite free

    free seed
      like result
        like isize
          cite free
        like c-1/error
        cite free

  task try-from, name <try_from>
    note <Try to create the target number type from a source number type. This returns an error if the source value is outside of the range of the target type.>

    cast c-1
      like isize
      like try-from
        like usize
          cite free

    take u
      like usize
        cite free
      cite free

    free seed
      like result
        like isize
          cite free
        like c-1/error
        cite free

  task fmt, name <fmt>
    note <Formats the value using the given formatter.>

    take self
      cite free
    take f
      like formatter
        time mask
      lock free

    free seed
      like result
        like form
        like error
          cite free
        cite free

  task fmt, name <fmt>
    note <Formats the value using the given formatter.>

    take self
      cite free
    take f
      like formatter
        time mask
      lock free

    free seed
      like result
        like form
        like error
          cite free
        cite free
