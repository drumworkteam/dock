
form any, name <std::any::Any>
  task is, name <is>
    note <Returns true if the inner type is the same as T.>

    lead t
      like any
        cite free

    head t

    take self
      cite free

    free seed
      like bool
        cite free

  task downcast-ref, name <downcast_ref>
    note <Returns some reference to the inner value if it is of type T, or None if it isn't.>

    lead t
      like any
        cite free

    head t

    take self
      cite free

    free seed
      like option
        like t
        cite free

  task downcast-mut, name <downcast_mut>
    note <Returns some mutable reference to the inner value if it is of type T, or None if it isn't.>

    lead t
      like any
        cite free

    head t

    take self
      cite free

    free seed
      like option
        like t
        cite free

  task downcast-ref-unchecked, name <downcast_ref_unchecked>
    note <Returns a reference to the inner value as type dyn T.>

    lead t
      like any
        cite free

    head t

    take self
      cite free

    free seed
      like t

  task downcast-mut-unchecked, name <downcast_mut_unchecked>
    note <Returns a mutable reference to the inner value as type dyn T.>

    lead t
      like any
        cite free

    head t

    take self
      cite free

    free seed
      like t

  task is, name <is>
    note <Forwards to the method defined on the type dyn Any.>

    lead t
      like any
        cite free

    head t

    take self
      cite free

    free seed
      like bool
        cite free

  task downcast-ref, name <downcast_ref>
    note <Forwards to the method defined on the type dyn Any.>

    lead t
      like any
        cite free

    head t

    take self
      cite free

    free seed
      like option
        like t
        cite free

  task downcast-mut, name <downcast_mut>
    note <Forwards to the method defined on the type dyn Any.>

    lead t
      like any
        cite free

    head t

    take self
      cite free

    free seed
      like option
        like t
        cite free

  task downcast-ref-unchecked, name <downcast_ref_unchecked>
    note <Forwards to the method defined on the type dyn Any.>

    lead t
      like any
        cite free

    head t

    take self
      cite free

    free seed
      like t

  task downcast-mut-unchecked, name <downcast_mut_unchecked>
    note <Forwards to the method defined on the type dyn Any.>

    lead t
      like any
        cite free

    head t

    take self
      cite free

    free seed
      like t

  task is, name <is>
    note <Forwards to the method defined on the type Any.>

    lead t
      like any
        cite free

    head t

    take self
      cite free

    free seed
      like bool
        cite free

  task downcast-ref, name <downcast_ref>
    note <Forwards to the method defined on the type Any.>

    lead t
      like any
        cite free

    head t

    take self
      cite free

    free seed
      like option
        like t
        cite free

  task downcast-mut, name <downcast_mut>
    note <Forwards to the method defined on the type Any.>

    lead t
      like any
        cite free

    head t

    take self
      cite free

    free seed
      like option
        like t
        cite free

  task downcast-ref-unchecked, name <downcast_ref_unchecked>
    note <Forwards to the method defined on the type Any.>

    lead t
      like any
        cite free

    head t

    take self
      cite free

    free seed
      like t

  task downcast-mut-unchecked, name <downcast_mut_unchecked>
    note <Forwards to the method defined on the type Any.>

    lead t
      like any
        cite free

    head t

    take self
      cite free

    free seed
      like t

  task fmt, name <fmt>
    note <Formats the value using the given formatter.>

    take self
      cite free
    take f
      like formatter
        time mask
      lock free

    free seed
      like result
        like form
        like error
          cite free
        cite free

  task fmt, name <fmt>
    note <Formats the value using the given formatter.>

    take self
      cite free
    take f
      like formatter
        time mask
      lock free

    free seed
      like result
        like form
        like error
          cite free
        cite free

  task fmt, name <fmt>
    note <Formats the value using the given formatter.>

    take self
      cite free
    take f
      like formatter
        time mask
      lock free

    free seed
      like result
        like form
        like error
          cite free
        cite free
