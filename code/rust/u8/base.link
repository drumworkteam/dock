
form u8
  task from-str-radix, name <from_str_radix>
    note <Converts a string slice in a given base to an integer.>

    take src
      like str
    take radix
      like u32
        cite free
      cite free

    free seed
      like result
        like u8
          cite free
        like parse-int-error
          cite free
        cite free

  task count-ones, name <count_ones>
    note <Returns the number of ones in the binary representation of self.>

    take self
      cite free

    free seed
      like u32
        cite free

  task count-zeros, name <count_zeros>
    note <Returns the number of zeros in the binary representation of self.>

    take self
      cite free

    free seed
      like u32
        cite free

  task leading-zeros, name <leading_zeros>
    note <Returns the number of leading zeros in the binary representation of self.>

    take self
      cite free

    free seed
      like u32
        cite free

  task trailing-zeros, name <trailing_zeros>
    note <Returns the number of trailing zeros in the binary representation of self.>

    take self
      cite free

    free seed
      like u32
        cite free

  task leading-ones, name <leading_ones>
    note <Returns the number of leading ones in the binary representation of self.>

    take self
      cite free

    free seed
      like u32
        cite free

  task trailing-ones, name <trailing_ones>
    note <Returns the number of trailing ones in the binary representation of self.>

    take self
      cite free

    free seed
      like u32
        cite free

  task rotate-left, name <rotate_left>
    note <Shifts the bits to the left by a specified amount, n, wrapping the truncated bits to the end of the resulting integer.>

    take self
      cite free
    take n
      like u32
        cite free
      cite free

    free seed
      like u8
        cite free

  task rotate-right, name <rotate_right>
    note <Shifts the bits to the right by a specified amount, n, wrapping the truncated bits to the beginning of the resulting integer.>

    take self
      cite free
    take n
      like u32
        cite free
      cite free

    free seed
      like u8
        cite free

  task swap-bytes, name <swap_bytes>
    note <Reverses the byte order of the integer.>

    take self
      cite free

    free seed
      like u8
        cite free

  task reverse-bits, name <reverse_bits>
    note <Reverses the order of bits in the integer. The least significant bit becomes the most significant bit, second least-significant bit becomes second most-significant bit, etc.>

    take self
      cite free

    free seed
      like u8
        cite free

  task from-be, name <from_be>
    note <Converts an integer from big endian to the target's endianness.>

    take x
      like u8
        cite free
      cite free

    free seed
      like u8
        cite free

  task from-le, name <from_le>
    note <Converts an integer from little endian to the target's endianness.>

    take x
      like u8
        cite free
      cite free

    free seed
      like u8
        cite free

  task to-be, name <to_be>
    note <Converts self to big endian from the target's endianness.>

    take self
      cite free

    free seed
      like u8
        cite free

  task to-le, name <to_le>
    note <Converts self to little endian from the target's endianness.>

    take self
      cite free

    free seed
      like u8
        cite free

  task checked-add, name <checked_add>
    note <Checked integer addition. Computes self + rhs, returning None if overflow occurred.>

    take self
      cite free
    take rhs
      like u8
        cite free
      cite free

    free seed
      like option
        like u8
          cite free
        cite free

  task unchecked-add, name <unchecked_add>
    note <Unchecked integer addition. Computes self + rhs, assuming overflow cannot occur.>

    take self
      cite free
    take rhs
      like u8
        cite free
      cite free

    free seed
      like u8
        cite free

  task checked-add-signed, name <checked_add_signed>
    note <Checked addition with a signed integer. Computes self + rhs, returning None if overflow occurred.>

    take self
      cite free
    take rhs
      like i8
        cite free
      cite free

    free seed
      like option
        like u8
          cite free
        cite free

  task checked-sub, name <checked_sub>
    note <Checked integer subtraction. Computes self - rhs, returning None if overflow occurred.>

    take self
      cite free
    take rhs
      like u8
        cite free
      cite free

    free seed
      like option
        like u8
          cite free
        cite free

  task unchecked-sub, name <unchecked_sub>
    note <Unchecked integer subtraction. Computes self - rhs, assuming overflow cannot occur.>

    take self
      cite free
    take rhs
      like u8
        cite free
      cite free

    free seed
      like u8
        cite free

  task checked-mul, name <checked_mul>
    note <Checked integer multiplication. Computes self * rhs, returning None if overflow occurred.>

    take self
      cite free
    take rhs
      like u8
        cite free
      cite free

    free seed
      like option
        like u8
          cite free
        cite free

  task unchecked-mul, name <unchecked_mul>
    note <Unchecked integer multiplication. Computes self * rhs, assuming overflow cannot occur.>

    take self
      cite free
    take rhs
      like u8
        cite free
      cite free

    free seed
      like u8
        cite free

  task checked-div, name <checked_div>
    note <Checked integer division. Computes self / rhs, returning None if rhs == 0.>

    take self
      cite free
    take rhs
      like u8
        cite free
      cite free

    free seed
      like option
        like u8
          cite free
        cite free

  task checked-div-euclid, name <checked_div_euclid>
    note <Checked Euclidean division. Computes self.div_euclid(rhs), returning None if rhs == 0.>

    take self
      cite free
    take rhs
      like u8
        cite free
      cite free

    free seed
      like option
        like u8
          cite free
        cite free

  task checked-rem, name <checked_rem>
    note <Checked integer remainder. Computes self % rhs, returning None if rhs == 0.>

    take self
      cite free
    take rhs
      like u8
        cite free
      cite free

    free seed
      like option
        like u8
          cite free
        cite free

  task checked-rem-euclid, name <checked_rem_euclid>
    note <Checked Euclidean modulo. Computes self.rem_euclid(rhs), returning None if rhs == 0.>

    take self
      cite free
    take rhs
      like u8
        cite free
      cite free

    free seed
      like option
        like u8
          cite free
        cite free

  task log, name <log>
    note <Returns the logarithm of the number with respect to an arbitrary base, rounded down.>

    take self
      cite free
    take base
      like u8
        cite free
      cite free

    free seed
      like u32
        cite free

  task log2, name <log2>
    note <Returns the base 2 logarithm of the number, rounded down.>

    take self
      cite free

    free seed
      like u32
        cite free

  task log10, name <log10>
    note <Returns the base 10 logarithm of the number, rounded down.>

    take self
      cite free

    free seed
      like u32
        cite free

  task checked-log, name <checked_log>
    note <Returns the logarithm of the number with respect to an arbitrary base, rounded down.>

    take self
      cite free
    take base
      like u8
        cite free
      cite free

    free seed
      like option
        like u32
          cite free
        cite free

  task checked-log2, name <checked_log2>
    note <Returns the base 2 logarithm of the number, rounded down.>

    take self
      cite free

    free seed
      like option
        like u32
          cite free
        cite free

  task checked-log10, name <checked_log10>
    note <Returns the base 10 logarithm of the number, rounded down.>

    take self
      cite free

    free seed
      like option
        like u32
          cite free
        cite free

  task checked-neg, name <checked_neg>
    note <Checked negation. Computes -self, returning None unless self == 0.>

    take self
      cite free

    free seed
      like option
        like u8
          cite free
        cite free

  task checked-shl, name <checked_shl>
    note <Checked shift left. Computes self &lt;&lt; rhs, returning None if rhs is larger than or equal to the number of bits in self.>

    take self
      cite free
    take rhs
      like u32
        cite free
      cite free

    free seed
      like option
        like u8
          cite free
        cite free

  task unchecked-shl, name <unchecked_shl>
    note <Unchecked shift left. Computes self &lt;&lt; rhs, assuming that rhs is less than the number of bits in self.>

    take self
      cite free
    take rhs
      like u8
        cite free
      cite free

    free seed
      like u8
        cite free

  task checked-shr, name <checked_shr>
    note <Checked shift right. Computes self &gt;&gt; rhs, returning None if rhs is larger than or equal to the number of bits in self.>

    take self
      cite free
    take rhs
      like u32
        cite free
      cite free

    free seed
      like option
        like u8
          cite free
        cite free

  task unchecked-shr, name <unchecked_shr>
    note <Unchecked shift right. Computes self &gt;&gt; rhs, assuming that rhs is less than the number of bits in self.>

    take self
      cite free
    take rhs
      like u8
        cite free
      cite free

    free seed
      like u8
        cite free

  task checked-pow, name <checked_pow>
    note <Checked exponentiation. Computes self.pow(exp), returning None if overflow occurred.>

    take self
      cite free
    take exp
      like u32
        cite free
      cite free

    free seed
      like option
        like u8
          cite free
        cite free

  task saturating-add, name <saturating_add>
    note <Saturating integer addition. Computes self + rhs, saturating at the numeric bounds instead of overflowing.>

    take self
      cite free
    take rhs
      like u8
        cite free
      cite free

    free seed
      like u8
        cite free

  task saturating-add-signed, name <saturating_add_signed>
    note <Saturating addition with a signed integer. Computes self + rhs, saturating at the numeric bounds instead of overflowing.>

    take self
      cite free
    take rhs
      like i8
        cite free
      cite free

    free seed
      like u8
        cite free

  task saturating-sub, name <saturating_sub>
    note <Saturating integer subtraction. Computes self - rhs, saturating at the numeric bounds instead of overflowing.>

    take self
      cite free
    take rhs
      like u8
        cite free
      cite free

    free seed
      like u8
        cite free

  task saturating-mul, name <saturating_mul>
    note <Saturating integer multiplication. Computes self * rhs, saturating at the numeric bounds instead of overflowing.>

    take self
      cite free
    take rhs
      like u8
        cite free
      cite free

    free seed
      like u8
        cite free

  task saturating-div, name <saturating_div>
    note <Saturating integer division. Computes self / rhs, saturating at the numeric bounds instead of overflowing.>

    take self
      cite free
    take rhs
      like u8
        cite free
      cite free

    free seed
      like u8
        cite free

  task saturating-pow, name <saturating_pow>
    note <Saturating integer exponentiation. Computes self.pow(exp), saturating at the numeric bounds instead of overflowing.>

    take self
      cite free
    take exp
      like u32
        cite free
      cite free

    free seed
      like u8
        cite free

  task wrapping-add, name <wrapping_add>
    note <Wrapping (modular) addition. Computes self + rhs, wrapping around at the boundary of the type.>

    take self
      cite free
    take rhs
      like u8
        cite free
      cite free

    free seed
      like u8
        cite free

  task wrapping-add-signed, name <wrapping_add_signed>
    note <Wrapping (modular) addition with a signed integer. Computes self + rhs, wrapping around at the boundary of the type.>

    take self
      cite free
    take rhs
      like i8
        cite free
      cite free

    free seed
      like u8
        cite free

  task wrapping-sub, name <wrapping_sub>
    note <Wrapping (modular) subtraction. Computes self - rhs, wrapping around at the boundary of the type.>

    take self
      cite free
    take rhs
      like u8
        cite free
      cite free

    free seed
      like u8
        cite free

  task wrapping-mul, name <wrapping_mul>
    note <Wrapping (modular) multiplication. Computes self * rhs, wrapping around at the boundary of the type.>

    take self
      cite free
    take rhs
      like u8
        cite free
      cite free

    free seed
      like u8
        cite free

  task wrapping-div, name <wrapping_div>
    note <Wrapping (modular) division. Computes self / rhs. Wrapped division on unsigned types is just normal division. There's no way wrapping could ever happen. This function exists, so that all operations are accounted for in the wrapping operations.>

    take self
      cite free
    take rhs
      like u8
        cite free
      cite free

    free seed
      like u8
        cite free

  task wrapping-div-euclid, name <wrapping_div_euclid>
    note <Wrapping Euclidean division. Computes self.div_euclid(rhs). Wrapped division on unsigned types is just normal division. There's no way wrapping could ever happen. This function exists, so that all operations are accounted for in the wrapping operations. Since, for the positive integers, all common definitions of division are equal, this is exactly equal to self.wrapping_div(rhs).>

    take self
      cite free
    take rhs
      like u8
        cite free
      cite free

    free seed
      like u8
        cite free

  task wrapping-rem, name <wrapping_rem>
    note <Wrapping (modular) remainder. Computes self % rhs. Wrapped remainder calculation on unsigned types is just the regular remainder calculation. There's no way wrapping could ever happen. This function exists, so that all operations are accounted for in the wrapping operations.>

    take self
      cite free
    take rhs
      like u8
        cite free
      cite free

    free seed
      like u8
        cite free

  task wrapping-rem-euclid, name <wrapping_rem_euclid>
    note <Wrapping Euclidean modulo. Computes self.rem_euclid(rhs). Wrapped modulo calculation on unsigned types is just the regular remainder calculation. There's no way wrapping could ever happen. This function exists, so that all operations are accounted for in the wrapping operations. Since, for the positive integers, all common definitions of division are equal, this is exactly equal to self.wrapping_rem(rhs).>

    take self
      cite free
    take rhs
      like u8
        cite free
      cite free

    free seed
      like u8
        cite free

  task wrapping-neg, name <wrapping_neg>
    note <Wrapping (modular) negation. Computes -self, wrapping around at the boundary of the type.>

    take self
      cite free

    free seed
      like u8
        cite free

  task wrapping-shl, name <wrapping_shl>
    note <Panic-free bitwise shift-left; yields self &lt;&lt; mask(rhs), where mask removes any high-order bits of rhs that would cause the shift to exceed the bitwidth of the type.>

    take self
      cite free
    take rhs
      like u32
        cite free
      cite free

    free seed
      like u8
        cite free

  task wrapping-shr, name <wrapping_shr>
    note <Panic-free bitwise shift-right; yields self &gt;&gt; mask(rhs), where mask removes any high-order bits of rhs that would cause the shift to exceed the bitwidth of the type.>

    take self
      cite free
    take rhs
      like u32
        cite free
      cite free

    free seed
      like u8
        cite free

  task wrapping-pow, name <wrapping_pow>
    note <Wrapping (modular) exponentiation. Computes self.pow(exp), wrapping around at the boundary of the type.>

    take self
      cite free
    take exp
      like u32
        cite free
      cite free

    free seed
      like u8
        cite free

  task overflowing-add, name <overflowing_add>
    note <Calculates self + rhs>

    take self
      cite free
    take rhs
      like u8
        cite free
      cite free

    free seed
      like form
        take t-1, like u8
        take t-2, like bool

  task carrying-add, name <carrying_add>
    note <Calculates self + rhs + carry without the ability to overflow.>

    take self
      cite free
    take rhs
      like u8
        cite free
      cite free
    take carry
      like bool
        cite free
      cite free

    free seed
      like form
        take t-1, like u8
        take t-2, like bool

  task overflowing-add-signed, name <overflowing_add_signed>
    note <Calculates self + rhs with a signed rhs>

    take self
      cite free
    take rhs
      like i8
        cite free
      cite free

    free seed
      like form
        take t-1, like u8
        take t-2, like bool

  task overflowing-sub, name <overflowing_sub>
    note <Calculates self - rhs>

    take self
      cite free
    take rhs
      like u8
        cite free
      cite free

    free seed
      like form
        take t-1, like u8
        take t-2, like bool

  task borrowing-sub, name <borrowing_sub>
    note <Calculates self - rhs - borrow without the ability to overflow.>

    take self
      cite free
    take rhs
      like u8
        cite free
      cite free
    take borrow
      like bool
        cite free
      cite free

    free seed
      like form
        take t-1, like u8
        take t-2, like bool

  task abs-diff, name <abs_diff>
    note <Computes the absolute difference between self and other.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like u8
        cite free

  task overflowing-mul, name <overflowing_mul>
    note <Calculates the multiplication of self and rhs.>

    take self
      cite free
    take rhs
      like u8
        cite free
      cite free

    free seed
      like form
        take t-1, like u8
        take t-2, like bool

  task overflowing-div, name <overflowing_div>
    note <Calculates the divisor when self is divided by rhs.>

    take self
      cite free
    take rhs
      like u8
        cite free
      cite free

    free seed
      like form
        take t-1, like u8
        take t-2, like bool

  task overflowing-div-euclid, name <overflowing_div_euclid>
    note <Calculates the quotient of Euclidean division self.div_euclid(rhs).>

    take self
      cite free
    take rhs
      like u8
        cite free
      cite free

    free seed
      like form
        take t-1, like u8
        take t-2, like bool

  task overflowing-rem, name <overflowing_rem>
    note <Calculates the remainder when self is divided by rhs.>

    take self
      cite free
    take rhs
      like u8
        cite free
      cite free

    free seed
      like form
        take t-1, like u8
        take t-2, like bool

  task overflowing-rem-euclid, name <overflowing_rem_euclid>
    note <Calculates the remainder self.rem_euclid(rhs) as if by Euclidean division.>

    take self
      cite free
    take rhs
      like u8
        cite free
      cite free

    free seed
      like form
        take t-1, like u8
        take t-2, like bool

  task overflowing-neg, name <overflowing_neg>
    note <Negates self in an overflowing fashion.>

    take self
      cite free

    free seed
      like form
        take t-1, like u8
        take t-2, like bool

  task overflowing-shl, name <overflowing_shl>
    note <Shifts self left by rhs bits.>

    take self
      cite free
    take rhs
      like u32
        cite free
      cite free

    free seed
      like form
        take t-1, like u8
        take t-2, like bool

  task overflowing-shr, name <overflowing_shr>
    note <Shifts self right by rhs bits.>

    take self
      cite free
    take rhs
      like u32
        cite free
      cite free

    free seed
      like form
        take t-1, like u8
        take t-2, like bool

  task overflowing-pow, name <overflowing_pow>
    note <Raises self to the power of exp, using exponentiation by squaring.>

    take self
      cite free
    take exp
      like u32
        cite free
      cite free

    free seed
      like form
        take t-1, like u8
        take t-2, like bool

  task pow, name <pow>
    note <Raises self to the power of exp, using exponentiation by squaring.>

    take self
      cite free
    take exp
      like u32
        cite free
      cite free

    free seed
      like u8
        cite free

  task div-euclid, name <div_euclid>
    note <Performs Euclidean division.>

    take self
      cite free
    take rhs
      like u8
        cite free
      cite free

    free seed
      like u8
        cite free

  task rem-euclid, name <rem_euclid>
    note <Calculates the least remainder of self (mod rhs).>

    take self
      cite free
    take rhs
      like u8
        cite free
      cite free

    free seed
      like u8
        cite free

  task div-floor, name <div_floor>
    note <Calculates the quotient of self and rhs, rounding the result towards negative infinity.>

    take self
      cite free
    take rhs
      like u8
        cite free
      cite free

    free seed
      like u8
        cite free

  task div-ceil, name <div_ceil>
    note <Calculates the quotient of self and rhs, rounding the result towards positive infinity.>

    take self
      cite free
    take rhs
      like u8
        cite free
      cite free

    free seed
      like u8
        cite free

  task next-multiple-of, name <next_multiple_of>
    note <Calculates the smallest value greater than or equal to self that is a multiple of rhs.>

    take self
      cite free
    take rhs
      like u8
        cite free
      cite free

    free seed
      like u8
        cite free

  task checked-next-multiple-of, name <checked_next_multiple_of>
    note <Calculates the smallest value greater than or equal to self that is a multiple of rhs. Returns None is rhs is zero or the operation would result in overflow.>

    take self
      cite free
    take rhs
      like u8
        cite free
      cite free

    free seed
      like option
        like u8
          cite free
        cite free

  task is-power-of-two, name <is_power_of_two>
    note <Returns true if and only if self == 2^k for some k.>

    take self
      cite free

    free seed
      like bool
        cite free

  task next-power-of-two, name <next_power_of_two>
    note <Returns the smallest power of two greater than or equal to self.>

    take self
      cite free

    free seed
      like u8
        cite free

  task checked-next-power-of-two, name <checked_next_power_of_two>
    note <Returns the smallest power of two greater than or equal to n. If the next power of two is greater than the type's maximum value, None is returned, otherwise the power of two is wrapped in Some.>

    take self
      cite free

    free seed
      like option
        like u8
          cite free
        cite free

  task wrapping-next-power-of-two, name <wrapping_next_power_of_two>
    note <Returns the smallest power of two greater than or equal to n. If the next power of two is greater than the type's maximum value, the return value is wrapped to 0.>

    take self
      cite free

    free seed
      like u8
        cite free

  task to-be-bytes, name <to_be_bytes>
    note <Return the memory representation of this integer as a byte array in big-endian (network) byte order.>

    take self
      cite free

    free seed
      like list
        like u8
        name 1

  task to-le-bytes, name <to_le_bytes>
    note <Return the memory representation of this integer as a byte array in little-endian byte order.>

    take self
      cite free

    free seed
      like list
        like u8
        name 1

  task to-ne-bytes, name <to_ne_bytes>
    note <Return the memory representation of this integer as a byte array in native byte order.>

    take self
      cite free

    free seed
      like list
        like u8
        name 1

  task from-be-bytes, name <from_be_bytes>
    note <Create a native endian integer value from its representation as a byte array in big endian.>

    take bytes
      like list
        like u8
        name 1
      cite free

    free seed
      like u8
        cite free

  task from-le-bytes, name <from_le_bytes>
    note <Create a native endian integer value from its representation as a byte array in little endian.>

    take bytes
      like list
        like u8
        name 1
      cite free

    free seed
      like u8
        cite free

  task from-ne-bytes, name <from_ne_bytes>
    note <Create a native endian integer value from its memory representation as a byte array in native endianness.>

    take bytes
      like list
        like u8
        name 1
      cite free

    free seed
      like u8
        cite free

  task min-value, name <min_value>
    note <New code should prefer to use u8::MIN instead.>

    free seed
      like u8
        cite free

  task max-value, name <max_value>
    note <New code should prefer to use u8::MAX instead.>

    free seed
      like u8
        cite free

  task widening-mul, name <widening_mul>
    note <Calculates the complete product self * rhs without the possibility to overflow.>

    take self
      cite free
    take rhs
      like u8
        cite free
      cite free

    free seed
      like form
        take t-1, like u8
        take t-2, like u8

  task carrying-mul, name <carrying_mul>
    note <Calculates the “full multiplication” self * rhs + carry without the possibility to overflow.>

    take self
      cite free
    take rhs
      like u8
        cite free
      cite free
    take carry
      like u8
        cite free
      cite free

    free seed
      like form
        take t-1, like u8
        take t-2, like u8

  task is-ascii, name <is_ascii>
    note <Checks if the value is within the ASCII range.>

    take self
      cite free

    free seed
      like bool
        cite free

  task to-ascii-uppercase, name <to_ascii_uppercase>
    note <Makes a copy of the value in its ASCII upper case equivalent.>

    take self
      cite free

    free seed
      like u8
        cite free

  task to-ascii-lowercase, name <to_ascii_lowercase>
    note <Makes a copy of the value in its ASCII lower case equivalent.>

    take self
      cite free

    free seed
      like u8
        cite free

  task eq-ignore-ascii-case, name <eq_ignore_ascii_case>
    note <Checks that two values are an ASCII case-insensitive match.>

    take self
      cite free
    take other
      like u8

    free seed
      like bool
        cite free

  task make-ascii-uppercase, name <make_ascii_uppercase>
    note <Converts this value to its ASCII upper case equivalent in-place.>

    take self
      cite free

  task make-ascii-lowercase, name <make_ascii_lowercase>
    note <Converts this value to its ASCII lower case equivalent in-place.>

    take self
      cite free

  task is-ascii-alphabetic, name <is_ascii_alphabetic>
    note <Checks if the value is an ASCII alphabetic character:>

    take self
      cite free

    free seed
      like bool
        cite free

  task is-ascii-uppercase, name <is_ascii_uppercase>
    note <Checks if the value is an ASCII uppercase character: U+0041 ‘A' ..= U+005A ‘Z'.>

    take self
      cite free

    free seed
      like bool
        cite free

  task is-ascii-lowercase, name <is_ascii_lowercase>
    note <Checks if the value is an ASCII lowercase character: U+0061 ‘a' ..= U+007A ‘z'.>

    take self
      cite free

    free seed
      like bool
        cite free

  task is-ascii-alphanumeric, name <is_ascii_alphanumeric>
    note <Checks if the value is an ASCII alphanumeric character:>

    take self
      cite free

    free seed
      like bool
        cite free

  task is-ascii-digit, name <is_ascii_digit>
    note <Checks if the value is an ASCII decimal digit: U+0030 ‘0' ..= U+0039 ‘9'.>

    take self
      cite free

    free seed
      like bool
        cite free

  task is-ascii-hexdigit, name <is_ascii_hexdigit>
    note <Checks if the value is an ASCII hexadecimal digit:>

    take self
      cite free

    free seed
      like bool
        cite free

  task is-ascii-punctuation, name <is_ascii_punctuation>
    note <Checks if the value is an ASCII punctuation character:>

    take self
      cite free

    free seed
      like bool
        cite free

  task is-ascii-graphic, name <is_ascii_graphic>
    note <Checks if the value is an ASCII graphic character: U+0021 ‘!' ..= U+007E ‘~'.>

    take self
      cite free

    free seed
      like bool
        cite free

  task is-ascii-whitespace, name <is_ascii_whitespace>
    note <Checks if the value is an ASCII whitespace character: U+0020 SPACE, U+0009 HORIZONTAL TAB, U+000A LINE FEED, U+000C FORM FEED, or U+000D CARRIAGE RETURN.>

    take self
      cite free

    free seed
      like bool
        cite free

  task is-ascii-control, name <is_ascii_control>
    note <Checks if the value is an ASCII control character: U+0000 NUL ..= U+001F UNIT SEPARATOR, or U+007F DELETE. Note that most ASCII whitespace characters are control characters, but SPACE is not.>

    take self
      cite free

    free seed
      like bool
        cite free

  task escape-ascii, name <escape_ascii>
    note <Returns an iterator that produces an escaped version of a u8, treating it as an ASCII character.>

    take self
      cite free

    free seed
      like escape-default
        cite free

  task add, name <add>
    note <Performs the + operation.>

    cast c-1
      like u8
      like add
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task add, name <add>
    note <Performs the + operation.>

    cast c-1
      like u8
      like add
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task add, name <add>
    note <Performs the + operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like u8
        cite free

  task add, name <add>
    note <Performs the + operation.>

    cast c-1
      like u8
      like add
        like u8
          cite free

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like c-1/output

  task add-assign, name <add_assign>
    note <Performs the += operation.>

    take self
      cite free
    take other
      like u8

  task add-assign, name <add_assign>
    note <Performs the += operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

  task is-ascii, name <is_ascii>
    note <Checks if the value is within the ASCII range.>

    take self
      cite free

    free seed
      like bool
        cite free

  task to-ascii-uppercase, name <to_ascii_uppercase>
    note <Makes a copy of the value in its ASCII upper case equivalent.>

    take self
      cite free

    free seed
      like self/owned

  task to-ascii-lowercase, name <to_ascii_lowercase>
    note <Makes a copy of the value in its ASCII lower case equivalent.>

    take self
      cite free

    free seed
      like self/owned

  task eq-ignore-ascii-case, name <eq_ignore_ascii_case>
    note <Checks that two values are an ASCII case-insensitive match.>

    take self
      cite free
    take o
      like self

    free seed
      like bool
        cite free

  task make-ascii-uppercase, name <make_ascii_uppercase>
    note <Converts this type to its ASCII upper case equivalent in-place.>

    take self
      cite free

  task make-ascii-lowercase, name <make_ascii_lowercase>
    note <Converts this type to its ASCII lower case equivalent in-place.>

    take self
      cite free

  task fmt, name <fmt>
    note <Formats the value using the given formatter.>

    take self
      cite free
    take f
      like formatter
        time mask
      lock free

    free seed
      like result
        like form
        like error
          cite free
        cite free

  task bitand, name <bitand>
    note <Performs the & operation.>

    cast c-1
      like u8
      like bit-and
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task bitand, name <bitand>
    note <Performs the & operation.>

    cast c-1
      like u8
      like bit-and
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task bitand, name <bitand>
    note <Performs the & operation.>

    take self
      cite free
    take rhs
      like u8
        cite free
      cite free

    free seed
      like u8
        cite free

  task bitand, name <bitand>
    note <Performs the & operation.>

    cast c-1
      like u8
      like bit-and
        like u8
          cite free

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like c-1/output

  task bitand-assign, name <bitand_assign>
    note <Performs the &= operation.>

    take self
      cite free
    take other
      like u8

  task bitand-assign, name <bitand_assign>
    note <Performs the &= operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

  task bitor, name <bitor>
    note <Performs the | operation.>

    cast c-1
      like u8
      like bit-or
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task bitor, name <bitor>
    note <Performs the | operation.>

    cast c-1
      like u8
      like bit-or
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task bitor, name <bitor>
    note <Performs the | operation.>

    cast c-1
      like u8
      like bit-or
        like non-zero-u8
          cite free

    take self
      cite free
    take rhs
      like non-zero-u8
        cite free
      cite free

    free seed
      like c-1/output

  task bitor, name <bitor>
    note <Performs the | operation.>

    cast c-1
      like u8
      like bit-or
        like u8
          cite free

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like c-1/output

  task bitor, name <bitor>
    note <Performs the | operation.>

    cast c-1
      like non-zero-u8
      like bit-or
        like u8
          cite free

    take self
      cite free
    take rhs
      like u8
        cite free
      cite free

    free seed
      like c-1/output

  task bitor, name <bitor>
    note <Performs the | operation.>

    take self
      cite free
    take rhs
      like u8
        cite free
      cite free

    free seed
      like u8
        cite free

  task bitor-assign, name <bitor_assign>
    note <Performs the |= operation.>

    take self
      cite free
    take other
      like u8

  task bitor-assign, name <bitor_assign>
    note <Performs the |= operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

  task bitor-assign, name <bitor_assign>
    note <Performs the |= operation.>

    take self
      cite free
    take rhs
      like u8
        cite free
      cite free

  task bitxor, name <bitxor>
    note <Performs the ^ operation.>

    cast c-1
      like u8
      like bit-xor
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task bitxor, name <bitxor>
    note <Performs the ^ operation.>

    cast c-1
      like u8
      like bit-xor
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task bitxor, name <bitxor>
    note <Performs the ^ operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like u8
        cite free

  task bitxor, name <bitxor>
    note <Performs the ^ operation.>

    cast c-1
      like u8
      like bit-xor
        like u8
          cite free

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like c-1/output

  task bitxor-assign, name <bitxor_assign>
    note <Performs the ^= operation.>

    take self
      cite free
    take other
      like u8

  task bitxor-assign, name <bitxor_assign>
    note <Performs the ^= operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

  task clone, name <clone>
    note <Returns a copy of the value.>

    take self
      cite free

    free seed
      like u8
        cite free

  task clone-from, name <clone_from>
    note <Performs copy-assignment from source.>

    take self
      cite free
    take source
      like self

  task fmt, name <fmt>
    note <Formats the value using the given formatter.>

    take self
      cite free
    take f
      like formatter
        time mask
      lock free

    free seed
      like result
        like form
        like error
          cite free
        cite free

  task default, name <default>
    note <Returns the default value of 0>

    free seed
      like u8
        cite free

  task fmt, name <fmt>
    note <Formats the value using the given formatter.>

    take self
      cite free
    take f
      like formatter
        time mask
      lock free

    free seed
      like result
        like form
        like error
          cite free
        cite free

  task div, name <div>
    note <Performs the / operation.>

    cast c-1
      like u8
      like div
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task div, name <div>
    note <Performs the / operation.>

    cast c-1
      like u8
      like div
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task div, name <div>
    note <This operation rounds towards zero, truncating any fractional part of the exact result, and cannot panic.>

    take self
      cite free
    take other
      like non-zero-u8
        cite free
      cite free

    free seed
      like u8
        cite free

  task div, name <div>
    note <Performs the / operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like u8
        cite free

  task div, name <div>
    note <Performs the / operation.>

    cast c-1
      like u8
      like div
        like u8
          cite free

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like c-1/output

  task div-assign, name <div_assign>
    note <Performs the /= operation.>

    take self
      cite free
    take other
      like u8

  task div-assign, name <div_assign>
    note <Performs the /= operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

  task from, name <from>
    note <Converts a NonZeroU8 into an u8>

    take nonzero
      like non-zero-u8
        cite free
      cite free

    free seed
      like u8
        cite free

  task from, name <from>
    note <Converts a bool to a u8. The resulting value is 0 for false and 1 for true values.>

    take small
      like bool
        cite free
      cite free

    free seed
      like u8
        cite free

  task from, name <from>
    note <Converts u8 to f32 losslessly.>

    take small
      like u8
        cite free
      cite free

    free seed
      like f32
        cite free

  task from, name <from>
    note <Converts u8 to i128 losslessly.>

    take small
      like u8
        cite free
      cite free

    free seed
      like i128
        cite free

  task from, name <from>
    note <Converts u8 to u32 losslessly.>

    take small
      like u8
        cite free
      cite free

    free seed
      like u32
        cite free

  task from, name <from>
    note <Converts a u8 into a char.>

    take i
      like u8
        cite free
      cite free

    free seed
      like char
        cite free

  task from, name <from>
    note <Converts u8 to isize losslessly.>

    take small
      like u8
        cite free
      cite free

    free seed
      like isize
        cite free

  task from, name <from>
    note <Converts an u8 into an AtomicU8.>

    take v
      like u8
        cite free
      cite free

    free seed
      like atomic-u8
        cite free

  task from, name <from>
    note <Converts u8 to f64 losslessly.>

    take small
      like u8
        cite free
      cite free

    free seed
      like f64
        cite free

  task from, name <from>
    note <Converts u8 to i64 losslessly.>

    take small
      like u8
        cite free
      cite free

    free seed
      like i64
        cite free

  task from, name <from>
    note <Converts u8 to u128 losslessly.>

    take small
      like u8
        cite free
      cite free

    free seed
      like u128
        cite free

  task from, name <from>
    note <Converts u8 to usize losslessly.>

    take small
      like u8
        cite free
      cite free

    free seed
      like usize
        cite free

  task from, name <from>
    note <Converts u8 to u64 losslessly.>

    take small
      like u8
        cite free
      cite free

    free seed
      like u64
        cite free

  task from, name <from>
    note <Converts u8 to u16 losslessly.>

    take small
      like u8
        cite free
      cite free

    free seed
      like u16
        cite free

  task from, name <from>
    note <Converts u8 to i32 losslessly.>

    take small
      like u8
        cite free
      cite free

    free seed
      like i32
        cite free

  task from, name <from>
    note <Converts u8 to i16 losslessly.>

    take small
      like u8
        cite free
      cite free

    free seed
      like i16
        cite free

  task from-str, name <from_str>
    note <Parses a string s to return a value of this type.>

    take src
      like str

    free seed
      like result
        like u8
          cite free
        like parse-int-error
          cite free
        cite free

  task hash, name <hash>
    note <Feeds this value into the given Hasher.>

    lead h
      like hasher
        cite free

    head h

    take self
      cite free
    take state
      like h
      lock free

  task hash-slice, name <hash_slice>
    note <Feeds a slice of this type into the given Hasher.>

    lead h
      like hasher
        cite free

    head h

    take data
      list u8
      cite free
    take state
      like h
      lock free

  task fmt, name <fmt>
    note <Formats the value using the given formatter.>

    take self
      cite free
    take f
      like formatter
        time mask
      lock free

    free seed
      like result
        like form
        like error
          cite free
        cite free

  task fmt, name <fmt>
    note <Formats the value using the given formatter.>

    take self
      cite free
    take f
      like formatter
        time mask
      lock free

    free seed
      like result
        like form
        like error
          cite free
        cite free

  task mul, name <mul>
    note <Performs the * operation.>

    cast c-1
      like u8
      like mul
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task mul, name <mul>
    note <Performs the * operation.>

    cast c-1
      like u8
      like mul
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task mul, name <mul>
    note <Performs the * operation.>

    cast c-1
      like u8
      like mul
        like u8
          cite free

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like c-1/output

  task mul, name <mul>
    note <Performs the * operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like u8
        cite free

  task mul-assign, name <mul_assign>
    note <Performs the *= operation.>

    take self
      cite free
    take other
      like u8

  task mul-assign, name <mul_assign>
    note <Performs the *= operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

  task not, name <not>
    note <Performs the unary ! operation.>

    take self
      cite free

    free seed
      like u8
        cite free

  task not, name <not>
    note <Performs the unary ! operation.>

    cast c-1
      like u8
      like not

    take self
      cite free

    free seed
      like c-1/output

  task fmt, name <fmt>
    note <Formats the value using the given formatter.>

    take self
      cite free
    take f
      like formatter
        time mask
      lock free

    free seed
      like result
        like form
        like error
          cite free
        cite free

  task cmp, name <cmp>
    note <This method returns an Ordering between self and other.>

    take self
      cite free
    take other
      like u8

    free seed
      like ordering
        cite free

  task max, name <max>
    note <Compares and returns the maximum of two values.>

    take self
      cite free
    take other
      like self
        cite free
      cite free

    free seed
      like self
        cite free

  task min, name <min>
    note <Compares and returns the minimum of two values.>

    take self
      cite free
    take other
      like self
        cite free
      cite free

    free seed
      like self
        cite free

  task clamp, name <clamp>
    note <Restrict a value to a certain interval.>

    take self
      cite free
    take min
      like self
        cite free
      cite free
    take max
      like self
        cite free
      cite free

    free seed
      like self
        cite free

  task eq, name <eq>
    note <This method tests for self and other values to be equal, and is used by ==.>

    take self
      cite free
    take other
      like u8

    free seed
      like bool
        cite free

  task ne, name <ne>
    note <This method tests for !=.>

    take self
      cite free
    take other
      like u8

    free seed
      like bool
        cite free

  task partial-cmp, name <partial_cmp>
    note <This method returns an ordering between self and other values if one exists.>

    take self
      cite free
    take other
      like u8

    free seed
      like option
        like ordering
          cite free
        cite free

  task lt, name <lt>
    note <This method tests less than (for self and other) and is used by the &lt; operator.>

    take self
      cite free
    take other
      like u8

    free seed
      like bool
        cite free

  task le, name <le>
    note <This method tests less than or equal to (for self and other) and is used by the &lt;= operator.>

    take self
      cite free
    take other
      like u8

    free seed
      like bool
        cite free

  task ge, name <ge>
    note <This method tests greater than or equal to (for self and other) and is used by the &gt;= operator.>

    take self
      cite free
    take other
      like u8

    free seed
      like bool
        cite free

  task gt, name <gt>
    note <This method tests greater than (for self and other) and is used by the &gt; operator.>

    take self
      cite free
    take other
      like u8

    free seed
      like bool
        cite free

  task product, name <product>
    note <Method which takes an iterator and generates Self from the elements by multiplying the items.>

    lead i
      like iterator
        like item
          cite free
          fall back
            like u8
              time a
        cite free

    head i

    take iter
      like i
        cite free
      cite free

    free seed
      like u8
        cite free

  task product, name <product>
    note <Method which takes an iterator and generates Self from the elements by multiplying the items.>

    lead i
      like iterator
        like item
          cite free
          fall back
            like u8
              cite free
        cite free

    head i

    take iter
      like i
        cite free
      cite free

    free seed
      like u8
        cite free

  task rem, name <rem>
    note <Performs the % operation.>

    cast c-1
      like u8
      like rem
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task rem, name <rem>
    note <Performs the % operation.>

    cast c-1
      like u8
      like rem
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task rem, name <rem>
    note <This operation satisfies n % d == n - (n / d) * d, and cannot panic.>

    take self
      cite free
    take other
      like non-zero-u8
        cite free
      cite free

    free seed
      like u8
        cite free

  task rem, name <rem>
    note <Performs the % operation.>

    cast c-1
      like u8
      like rem
        like u8
          cite free

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like c-1/output

  task rem, name <rem>
    note <Performs the % operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like u8
        cite free

  task rem-assign, name <rem_assign>
    note <Performs the %= operation.>

    take self
      cite free
    take other
      like u8

  task rem-assign, name <rem_assign>
    note <Performs the %= operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u8
      like shl
        like i128
          cite free

    take self
      cite free
    take other
      like i128

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u8
      like shl
        like i128
          cite free

    take self
      cite free
    take other
      like i128

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u8
      like shl
        like i16
          cite free

    take self
      cite free
    take other
      like i16

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u8
      like shl
        like i16
          cite free

    take self
      cite free
    take other
      like i16

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u8
      like shl
        like i32
          cite free

    take self
      cite free
    take other
      like i32

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u8
      like shl
        like i32
          cite free

    take self
      cite free
    take other
      like i32

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u8
      like shl
        like i64
          cite free

    take self
      cite free
    take other
      like i64

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u8
      like shl
        like i64
          cite free

    take self
      cite free
    take other
      like i64

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u8
      like shl
        like i8
          cite free

    take self
      cite free
    take other
      like i8

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u8
      like shl
        like i8
          cite free

    take self
      cite free
    take other
      like i8

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u8
      like shl
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u8
      like shl
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u8
      like shl
        like u128
          cite free

    take self
      cite free
    take other
      like u128

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u8
      like shl
        like u128
          cite free

    take self
      cite free
    take other
      like u128

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u8
      like shl
        like u16
          cite free

    take self
      cite free
    take other
      like u16

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u8
      like shl
        like u16
          cite free

    take self
      cite free
    take other
      like u16

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u8
      like shl
        like u32
          cite free

    take self
      cite free
    take other
      like u32

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u8
      like shl
        like u32
          cite free

    take self
      cite free
    take other
      like u32

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u8
      like shl
        like u64
          cite free

    take self
      cite free
    take other
      like u64

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u8
      like shl
        like u64
          cite free

    take self
      cite free
    take other
      like u64

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like i8
      like shl
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like isize
      like shl
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like i16
      like shl
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u128
      like shl
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like isize
      like shl
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like usize
      like shl
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u8
      like shl
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like i64
      like shl
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like i8
      like shl
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like usize
      like shl
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u32
      like shl
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u16
      like shl
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u8
      like shl
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u32
      like shl
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like i64
      like shl
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like i16
      like shl
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u64
      like shl
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like i128
      like shl
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like i32
      like shl
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u16
      like shl
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u128
      like shl
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u64
      like shl
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like i128
      like shl
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like i32
      like shl
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u8
      like shl
        like usize
          cite free

    take self
      cite free
    take other
      like usize

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u8
      like shl
        like usize
          cite free

    take self
      cite free
    take other
      like usize

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    take self
      cite free
    take other
      like i128
        cite free
      cite free

    free seed
      like u8
        cite free

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u8
      like shl
        like i128
          cite free

    take self
      cite free
    take other
      like i128
        cite free
      cite free

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    take self
      cite free
    take other
      like i16
        cite free
      cite free

    free seed
      like u8
        cite free

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u8
      like shl
        like i16
          cite free

    take self
      cite free
    take other
      like i16
        cite free
      cite free

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    take self
      cite free
    take other
      like i32
        cite free
      cite free

    free seed
      like u8
        cite free

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u8
      like shl
        like i32
          cite free

    take self
      cite free
    take other
      like i32
        cite free
      cite free

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    take self
      cite free
    take other
      like i64
        cite free
      cite free

    free seed
      like u8
        cite free

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u8
      like shl
        like i64
          cite free

    take self
      cite free
    take other
      like i64
        cite free
      cite free

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    take self
      cite free
    take other
      like i8
        cite free
      cite free

    free seed
      like u8
        cite free

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u8
      like shl
        like i8
          cite free

    take self
      cite free
    take other
      like i8
        cite free
      cite free

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like u8
        cite free

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u8
      like shl
        like isize
          cite free

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u8
      like shl
        like u128
          cite free

    take self
      cite free
    take other
      like u128
        cite free
      cite free

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    take self
      cite free
    take other
      like u128
        cite free
      cite free

    free seed
      like u8
        cite free

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u8
      like shl
        like u16
          cite free

    take self
      cite free
    take other
      like u16
        cite free
      cite free

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    take self
      cite free
    take other
      like u16
        cite free
      cite free

    free seed
      like u8
        cite free

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    take self
      cite free
    take other
      like u32
        cite free
      cite free

    free seed
      like u8
        cite free

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u8
      like shl
        like u32
          cite free

    take self
      cite free
    take other
      like u32
        cite free
      cite free

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    take self
      cite free
    take other
      like u64
        cite free
      cite free

    free seed
      like u8
        cite free

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u8
      like shl
        like u64
          cite free

    take self
      cite free
    take other
      like u64
        cite free
      cite free

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like isize
        cite free

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like usize
      like shl
        like u8
          cite free

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like u8
        cite free

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like i16
        cite free

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like i16
      like shl
        like u8
          cite free

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like i64
        cite free

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like i128
        cite free

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like u128
        cite free

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like i128
      like shl
        like u8
          cite free

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like isize
      like shl
        like u8
          cite free

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like i8
        cite free

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like i32
      like shl
        like u8
          cite free

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u128
      like shl
        like u8
          cite free

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like u32
        cite free

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u64
      like shl
        like u8
          cite free

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like u64
        cite free

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u16
      like shl
        like u8
          cite free

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like u16
        cite free

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like i8
      like shl
        like u8
          cite free

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u8
      like shl
        like u8
          cite free

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like usize
        cite free

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like i64
      like shl
        like u8
          cite free

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like i32
        cite free

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u32
      like shl
        like u8
          cite free

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    cast c-1
      like u8
      like shl
        like usize
          cite free

    take self
      cite free
    take other
      like usize
        cite free
      cite free

    free seed
      like c-1/output

  task shl, name <shl>
    note <Performs the &lt;&lt; operation.>

    take self
      cite free
    take other
      like usize
        cite free
      cite free

    free seed
      like u8
        cite free

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like i128

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like i16

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like i32

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like i64

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like i8

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like isize

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like u128

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like u16

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like u32

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like u64

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like u8

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like u8

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like u8

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like u8

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like u8

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like u8

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like u8

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like u8

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like u8

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like u8

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like u8

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like u8

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like usize

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like i128
        cite free
      cite free

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like i16
        cite free
      cite free

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like i32
        cite free
      cite free

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like i64
        cite free
      cite free

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like i8
        cite free
      cite free

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like u128
        cite free
      cite free

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like u16
        cite free
      cite free

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like u32
        cite free
      cite free

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like u64
        cite free
      cite free

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

  task shl-assign, name <shl_assign>
    note <Performs the &lt;&lt;= operation.>

    take self
      cite free
    take other
      like usize
        cite free
      cite free

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u8
      like shr
        like i128
          cite free

    take self
      cite free
    take other
      like i128

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u8
      like shr
        like i128
          cite free

    take self
      cite free
    take other
      like i128

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u8
      like shr
        like i16
          cite free

    take self
      cite free
    take other
      like i16

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u8
      like shr
        like i16
          cite free

    take self
      cite free
    take other
      like i16

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u8
      like shr
        like i32
          cite free

    take self
      cite free
    take other
      like i32

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u8
      like shr
        like i32
          cite free

    take self
      cite free
    take other
      like i32

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u8
      like shr
        like i64
          cite free

    take self
      cite free
    take other
      like i64

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u8
      like shr
        like i64
          cite free

    take self
      cite free
    take other
      like i64

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u8
      like shr
        like i8
          cite free

    take self
      cite free
    take other
      like i8

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u8
      like shr
        like i8
          cite free

    take self
      cite free
    take other
      like i8

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u8
      like shr
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u8
      like shr
        like isize
          cite free

    take self
      cite free
    take other
      like isize

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u8
      like shr
        like u128
          cite free

    take self
      cite free
    take other
      like u128

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u8
      like shr
        like u128
          cite free

    take self
      cite free
    take other
      like u128

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u8
      like shr
        like u16
          cite free

    take self
      cite free
    take other
      like u16

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u8
      like shr
        like u16
          cite free

    take self
      cite free
    take other
      like u16

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u8
      like shr
        like u32
          cite free

    take self
      cite free
    take other
      like u32

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u8
      like shr
        like u32
          cite free

    take self
      cite free
    take other
      like u32

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u8
      like shr
        like u64
          cite free

    take self
      cite free
    take other
      like u64

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u8
      like shr
        like u64
          cite free

    take self
      cite free
    take other
      like u64

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like i8
      like shr
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like i32
      like shr
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like i32
      like shr
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u16
      like shr
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u32
      like shr
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like isize
      like shr
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u32
      like shr
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like i64
      like shr
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u64
      like shr
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u8
      like shr
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u16
      like shr
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like i128
      like shr
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like i64
      like shr
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like usize
      like shr
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u64
      like shr
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like i16
      like shr
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u128
      like shr
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u128
      like shr
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like i128
      like shr
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like usize
      like shr
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like i16
      like shr
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u8
      like shr
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like isize
      like shr
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like i8
      like shr
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u8
      like shr
        like usize
          cite free

    take self
      cite free
    take other
      like usize

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u8
      like shr
        like usize
          cite free

    take self
      cite free
    take other
      like usize

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u8
      like shr
        like i128
          cite free

    take self
      cite free
    take other
      like i128
        cite free
      cite free

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    take self
      cite free
    take other
      like i128
        cite free
      cite free

    free seed
      like u8
        cite free

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    take self
      cite free
    take other
      like i16
        cite free
      cite free

    free seed
      like u8
        cite free

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u8
      like shr
        like i16
          cite free

    take self
      cite free
    take other
      like i16
        cite free
      cite free

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u8
      like shr
        like i32
          cite free

    take self
      cite free
    take other
      like i32
        cite free
      cite free

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    take self
      cite free
    take other
      like i32
        cite free
      cite free

    free seed
      like u8
        cite free

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    take self
      cite free
    take other
      like i64
        cite free
      cite free

    free seed
      like u8
        cite free

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u8
      like shr
        like i64
          cite free

    take self
      cite free
    take other
      like i64
        cite free
      cite free

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    take self
      cite free
    take other
      like i8
        cite free
      cite free

    free seed
      like u8
        cite free

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u8
      like shr
        like i8
          cite free

    take self
      cite free
    take other
      like i8
        cite free
      cite free

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u8
      like shr
        like isize
          cite free

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

    free seed
      like u8
        cite free

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u8
      like shr
        like u128
          cite free

    take self
      cite free
    take other
      like u128
        cite free
      cite free

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    take self
      cite free
    take other
      like u128
        cite free
      cite free

    free seed
      like u8
        cite free

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u8
      like shr
        like u16
          cite free

    take self
      cite free
    take other
      like u16
        cite free
      cite free

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    take self
      cite free
    take other
      like u16
        cite free
      cite free

    free seed
      like u8
        cite free

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    take self
      cite free
    take other
      like u32
        cite free
      cite free

    free seed
      like u8
        cite free

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u8
      like shr
        like u32
          cite free

    take self
      cite free
    take other
      like u32
        cite free
      cite free

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    take self
      cite free
    take other
      like u64
        cite free
      cite free

    free seed
      like u8
        cite free

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u8
      like shr
        like u64
          cite free

    take self
      cite free
    take other
      like u64
        cite free
      cite free

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like usize
        cite free

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like i8
      like shr
        like u8
          cite free

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like u64
        cite free

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like isize
        cite free

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like u16
        cite free

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like i128
      like shr
        like u8
          cite free

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like i16
      like shr
        like u8
          cite free

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like i16
        cite free

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like u128
        cite free

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like i32
      like shr
        like u8
          cite free

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like i64
        cite free

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u8
      like shr
        like u8
          cite free

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like u8
        cite free

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like i32
        cite free

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like u32
        cite free

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u64
      like shr
        like u8
          cite free

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like usize
      like shr
        like u8
          cite free

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like i128
        cite free

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like i8
        cite free

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u16
      like shr
        like u8
          cite free

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like i64
      like shr
        like u8
          cite free

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like isize
      like shr
        like u8
          cite free

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u128
      like shr
        like u8
          cite free

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u32
      like shr
        like u8
          cite free

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    cast c-1
      like u8
      like shr
        like usize
          cite free

    take self
      cite free
    take other
      like usize
        cite free
      cite free

    free seed
      like c-1/output

  task shr, name <shr>
    note <Performs the &gt;&gt; operation.>

    take self
      cite free
    take other
      like usize
        cite free
      cite free

    free seed
      like u8
        cite free

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like i128

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like i16

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like i32

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like i64

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like i8

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like isize

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like u128

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like u16

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like u32

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like u64

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like u8

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like u8

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like u8

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like u8

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like u8

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like u8

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like u8

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like u8

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like u8

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like u8

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like u8

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like u8

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like usize

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like i128
        cite free
      cite free

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like i16
        cite free
      cite free

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like i32
        cite free
      cite free

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like i64
        cite free
      cite free

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like i8
        cite free
      cite free

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like isize
        cite free
      cite free

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like u128
        cite free
      cite free

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like u16
        cite free
      cite free

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like u32
        cite free
      cite free

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like u64
        cite free
      cite free

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

  task shr-assign, name <shr_assign>
    note <Performs the &gt;&gt;= operation.>

    take self
      cite free
    take other
      like usize
        cite free
      cite free

  task forward-unchecked, name <forward_unchecked>
    note <Returns the value that would be obtained by taking the successor of self count times.>

    take start
      like u8
        cite free
      cite free
    take n
      like usize
        cite free
      cite free

    free seed
      like u8
        cite free

  task backward-unchecked, name <backward_unchecked>
    note <Returns the value that would be obtained by taking the predecessor of self count times.>

    take start
      like u8
        cite free
      cite free
    take n
      like usize
        cite free
      cite free

    free seed
      like u8
        cite free

  task forward, name <forward>
    note <Returns the value that would be obtained by taking the successor of self count times.>

    take start
      like u8
        cite free
      cite free
    take n
      like usize
        cite free
      cite free

    free seed
      like u8
        cite free

  task backward, name <backward>
    note <Returns the value that would be obtained by taking the predecessor of self count times.>

    take start
      like u8
        cite free
      cite free
    take n
      like usize
        cite free
      cite free

    free seed
      like u8
        cite free

  task steps-between, name <steps_between>
    note <Returns the number of successor steps required to get from start to end.>

    take start
      like u8
    take end
      like u8

    free seed
      like option
        like usize
          cite free
        cite free

  task forward-checked, name <forward_checked>
    note <Returns the value that would be obtained by taking the successor of self count times.>

    take start
      like u8
        cite free
      cite free
    take n
      like usize
        cite free
      cite free

    free seed
      like option
        like u8
          cite free
        cite free

  task backward-checked, name <backward_checked>
    note <Returns the value that would be obtained by taking the predecessor of self count times.>

    take start
      like u8
        cite free
      cite free
    take n
      like usize
        cite free
      cite free

    free seed
      like option
        like u8
          cite free
        cite free

  task sub, name <sub>
    note <Performs the - operation.>

    cast c-1
      like u8
      like sub
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task sub, name <sub>
    note <Performs the - operation.>

    cast c-1
      like u8
      like sub
        like u8
          cite free

    take self
      cite free
    take other
      like u8

    free seed
      like c-1/output

  task sub, name <sub>
    note <Performs the - operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like u8
        cite free

  task sub, name <sub>
    note <Performs the - operation.>

    cast c-1
      like u8
      like sub
        like u8
          cite free

    take self
      cite free
    take other
      like u8
        cite free
      cite free

    free seed
      like c-1/output

  task sub-assign, name <sub_assign>
    note <Performs the -= operation.>

    take self
      cite free
    take other
      like u8

  task sub-assign, name <sub_assign>
    note <Performs the -= operation.>

    take self
      cite free
    take other
      like u8
        cite free
      cite free

  task sum, name <sum>
    note <Method which takes an iterator and generates Self from the elements by “summing up” the items.>

    lead i
      like iterator
        like item
          cite free
          fall back
            like u8
              time a
        cite free

    head i

    take iter
      like i
        cite free
      cite free

    free seed
      like u8
        cite free

  task sum, name <sum>
    note <Method which takes an iterator and generates Self from the elements by “summing up” the items.>

    lead i
      like iterator
        like item
          cite free
          fall back
            like u8
              cite free
        cite free

    head i

    take iter
      like i
        cite free
      cite free

    free seed
      like u8
        cite free

  task to-string, name <to_string>
    note <Converts the given value to a String.>

    take self
      cite free

    free seed
      like string
        cite free

  task try-from, name <try_from>
    note <Performs the conversion.>

    cast c-1
      like u8
      like try-from
        like char
          cite free

    take c
      like char
        cite free
      cite free

    free seed
      like result
        like u8
          cite free
        like c-1/error
        cite free

  task try-from, name <try_from>
    note <Try to create the target number type from a source number type. This returns an error if the source value is outside of the range of the target type.>

    cast c-1
      like u8
      like try-from
        like i128
          cite free

    take u
      like i128
        cite free
      cite free

    free seed
      like result
        like u8
          cite free
        like c-1/error
        cite free

  task try-from, name <try_from>
    note <Try to create the target number type from a source number type. This returns an error if the source value is outside of the range of the target type.>

    cast c-1
      like u8
      like try-from
        like i16
          cite free

    take u
      like i16
        cite free
      cite free

    free seed
      like result
        like u8
          cite free
        like c-1/error
        cite free

  task try-from, name <try_from>
    note <Try to create the target number type from a source number type. This returns an error if the source value is outside of the range of the target type.>

    cast c-1
      like u8
      like try-from
        like i32
          cite free

    take u
      like i32
        cite free
      cite free

    free seed
      like result
        like u8
          cite free
        like c-1/error
        cite free

  task try-from, name <try_from>
    note <Try to create the target number type from a source number type. This returns an error if the source value is outside of the range of the target type.>

    cast c-1
      like u8
      like try-from
        like i64
          cite free

    take u
      like i64
        cite free
      cite free

    free seed
      like result
        like u8
          cite free
        like c-1/error
        cite free

  task try-from, name <try_from>
    note <Try to create the target number type from a source number type. This returns an error if the source value is outside of the range of the target type.>

    cast c-1
      like u8
      like try-from
        like i8
          cite free

    take u
      like i8
        cite free
      cite free

    free seed
      like result
        like u8
          cite free
        like c-1/error
        cite free

  task try-from, name <try_from>
    note <Try to create the target number type from a source number type. This returns an error if the source value is outside of the range of the target type.>

    cast c-1
      like u8
      like try-from
        like isize
          cite free

    take u
      like isize
        cite free
      cite free

    free seed
      like result
        like u8
          cite free
        like c-1/error
        cite free

  task try-from, name <try_from>
    note <Try to create the target number type from a source number type. This returns an error if the source value is outside of the range of the target type.>

    cast c-1
      like u8
      like try-from
        like u128
          cite free

    take u
      like u128
        cite free
      cite free

    free seed
      like result
        like u8
          cite free
        like c-1/error
        cite free

  task try-from, name <try_from>
    note <Try to create the target number type from a source number type. This returns an error if the source value is outside of the range of the target type.>

    cast c-1
      like u8
      like try-from
        like u16
          cite free

    take u
      like u16
        cite free
      cite free

    free seed
      like result
        like u8
          cite free
        like c-1/error
        cite free

  task try-from, name <try_from>
    note <Try to create the target number type from a source number type. This returns an error if the source value is outside of the range of the target type.>

    cast c-1
      like u8
      like try-from
        like u32
          cite free

    take u
      like u32
        cite free
      cite free

    free seed
      like result
        like u8
          cite free
        like c-1/error
        cite free

  task try-from, name <try_from>
    note <Try to create the target number type from a source number type. This returns an error if the source value is outside of the range of the target type.>

    cast c-1
      like u8
      like try-from
        like u64
          cite free

    take u
      like u64
        cite free
      cite free

    free seed
      like result
        like u8
          cite free
        like c-1/error
        cite free

  task try-from, name <try_from>
    note <Try to create the target number type from a source number type. This returns an error if the source value is outside of the range of the target type.>

    cast c-1
      like i8
      like try-from
        like u8
          cite free

    take u
      like u8
        cite free
      cite free

    free seed
      like result
        like i8
          cite free
        like c-1/error
        cite free

  task try-from, name <try_from>
    note <Attempts to convert u8 to NonZeroU8.>

    cast c-1
      like non-zero-u8
      like try-from
        like u8
          cite free

    take value
      like u8
        cite free
      cite free

    free seed
      like result
        like non-zero-u8
          cite free
        like c-1/error
        cite free

  task try-from, name <try_from>
    note <Try to create the target number type from a source number type. This returns an error if the source value is outside of the range of the target type.>

    cast c-1
      like u8
      like try-from
        like usize
          cite free

    take u
      like usize
        cite free
      cite free

    free seed
      like result
        like u8
          cite free
        like c-1/error
        cite free

  task fmt, name <fmt>
    note <Formats the value using the given formatter.>

    take self
      cite free
    take f
      like formatter
        time mask
      lock free

    free seed
      like result
        like form
        like error
          cite free
        cite free

  task fmt, name <fmt>
    note <Formats the value using the given formatter.>

    take self
      cite free
    take f
      like formatter
        time mask
      lock free

    free seed
      like result
        like form
        like error
          cite free
        cite free
